<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.ems.mapper.SysRoleAuthorityObjectMapper">

    <resultMap type="com.platform.ems.domain.SysRoleAuthorityObject" id="SysRoleAuthorityObjectResult">
        <result property="clientId" column="client_id"/>
        <result property="roleAuthorityObjectSid" column="role_authority_object_sid"/>
        <result property="roleId" column="role_id"/>
        <result property="authorityObjectSid" column="authority_object_sid"/>
        <result property="authorityObjectCode" column="authority_object_code"/>
        <result property="remark" column="remark"/>
        <result property="creatorAccount" column="creator_account"/>
        <result property="createDate" column="create_date"/>
        <result property="updaterAccount" column="updater_account"/>
        <result property="updateDate" column="update_date"/>
        <result property="dataSourceSys" column="data_source_sys"/>
        <result property="creatorAccountName" column="creator_account_name"/>
        <result property="updaterAccountName" column="updater_account_name"/>
    </resultMap>

    <sql id="selectSysRoleAuthorityObjectVo">
        select t.client_id,
               t.role_authority_object_sid,
               t.role_id,
               t.authority_object_sid,
               t.authority_object_code,
               t.remark,
               t.creator_account,
               t.create_date,
               t.updater_account,
               t.update_date,
               t.data_source_sys,
               t2.nick_name AS creator_account_name,
               t3.nick_name AS updater_account_name,
        from s_sys_role_authority_object t
        left join sys_user t2 on  t.creator_account = t2.user_name
        left join sys_user t3 on  t.updater_account = t3.user_name
    </sql>

    <select id="selectSysRoleAuthorityObjectById" parameterType="Long" resultMap="SysRoleAuthorityObjectResult">
        <include refid="selectSysRoleAuthorityObjectVo"/>
        where t.role_authority_object_sid = #{roleAuthorityObjectSid}
    </select>


    <select id="selectSysRoleAuthorityObjectList" parameterType="com.platform.ems.domain.SysRoleAuthorityObject"
            resultMap="SysRoleAuthorityObjectResult">
        <include refid="selectSysRoleAuthorityObjectVo"/>
        <where>
            <if test="clientId != null  and clientId != ''">
                and (<foreach collection="clientId.split(';')" item="item" separator="or">t.client_id like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="roleId != null ">
                and t.role_id =#{roleId}
            </if>
            <if test="authorityObjectSid != null ">
                and t.authority_object_sid =#{authorityObjectSid}
            </if>
            <if test="authorityObjectCode != null and authorityObjectCode != ''">
                and t.authority_object_code =#{authorityObjectCode}
            </if>
            <if test="creatorAccount != null  and creatorAccount != ''">
                and (<foreach collection="creatorAccount.split(';')" item="item" separator="or">t.creator_account like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="beginTime != null and beginTime!=''">
                and date_format(t.create_date,'%y%m%d') &gt;= date_format(#{beginTime},'%y%m%d')
            </if>
            <if test="endTime != null and endTime!=''">
                and date_format(t.create_date,'%y%m%d') &lt;= date_format(#{endTime},'%y%m%d')
            </if>
            <if test="updaterAccount != null  and updaterAccount != ''">
                and (<foreach collection="updaterAccount.split(';')" item="item" separator="or">t.updater_account like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="updateDate != null ">
                and t.update_date = #{updateDate}
            </if>
            <if test="dataSourceSys != null  and dataSourceSys != ''">
                and (<foreach collection="dataSourceSys.split(';')" item="item" separator="or">t.data_source_sys like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="params.dataScope != null and params.dataScope!=''">
                ${params.dataScope}
            </if>
        </where>
    </select>


    <!--添加多个-->
    <insert id="inserts">
        insert into s_sys_role_authority_object
        <trim prefix="(" suffix=")" suffixOverrides=",">
            client_id,
            role_authority_object_sid,
            role_id,
            authority_object_sid,
            authority_object_code,
            remark,
            creator_account,
            create_date,
            updater_account,
            update_date,
            data_source_sys,
        </trim>
        values
        <foreach collection="list" item="item" separator=",">
            <trim prefix=" (" suffix=")" suffixOverrides=",">
                #{item.clientId},
                #{item.roleId},
                #{item.authorityObjectSid},
                #{item.authorityObjectCode},
                #{item.remark},
                #{item.creatorAccount},
                #{item.createDate},
                #{item.updaterAccount},
                #{item.updateDate},
                #{item.dataSourceSys},
            </trim>
        </foreach>
    </insert>

    <!--更新-->
    <update id="updateAllById">
        update s_sys_role_authority_object
        <trim prefix="SET" suffixOverrides=",">
            client_id = #{clientId},
            role_authority_object_sid = #{roleAuthorityObjectSid},
            role_id = #{roleId},
            authority_object_sid = #{authorityObjectSid},
            authority_object_code = #{authorityObjectCode},
            remark = #{remark},
            creator_account = #{creatorAccount},
            create_date = #{createDate},
            updater_account = #{updaterAccount},
            update_date = #{updateDate},
            data_source_sys = #{dataSourceSys},
        </trim>
        where role_authority_object_sid = #{roleAuthorityObjectSid}
    </update>

    <!--更新多个-->
    <update id="updatesAllById">
        <foreach collection="list" item="o" separator=";">
            update s_sys_role_authority_object
            <trim prefix="SET" suffixOverrides=",">
                client_id = #{o.clientId},
                role_authority_object_sid = #{o.roleAuthorityObjectSid},
                role_id = #{o.roleId},
                authority_object_sid = #{o.authorityObjectSid},
                authority_object_code = #{o.authorityObjectCode},
                remark = #{o.remark},
                creator_account = #{o.creatorAccount},
                create_date = #{o.createDate},
                updater_account = #{o.updaterAccount},
                update_date = #{o.updateDate},
                data_source_sys = #{o.dataSourceSys},
            </trim>
            where role_authority_object_sid = #{roleAuthorityObjectSid}
        </foreach>
    </update>

</mapper>
