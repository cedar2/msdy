<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.ems.plug.mapper.ConBcstUserConfigMapper">

    <resultMap type="com.platform.ems.plug.domain.ConBcstUserConfig" id="ConBcstUserConfigResult">
        <result property="clientId" column="client_id"/>
        <result property="sid" column="sid"/>
        <result property="dataobjectCategorySid" column="dataobject_category_sid"/>
        <result property="dataobjectCategoryCode" column="dataobject_category_code"/>
        <result property="bcstType" column="bcst_type"/>
        <result property="userAccount" column="user_account"/>
        <result property="sort" column="sort"/>
        <result property="remark" column="remark"/>
        <result property="handleStatus" column="handle_status"/>
        <result property="creatorAccount" column="creator_account"/>
        <result property="createDate" column="create_date"/>
        <result property="updaterAccount" column="updater_account"/>
        <result property="updateDate" column="update_date"/>
        <result property="confirmerAccount" column="confirmer_account"/>
        <result property="confirmDate" column="confirm_date"/>
        <result property="dataSourceSys" column="data_source_sys"/>
        <result property="creatorAccountName" column="creator_account_name"/>
        <result property="dataobjectCategoryName" column="dataobject_category_name"/>
        <result property="userAccountName" column="user_account_name"/>
        <result property="userId" column="user_id"/>
        <result property="email" column="email"/>
        <result property="workWechatOpenid" column="work_wechat_openid"/>
        <result property="dingtalkOpenid" column="dingtalk_openid"/>
        <result property="wxGzhOpenid" column="wx_gzh_openid"/>
        <result property="wxXcxOpenid" column="wx_xcx_openid"/>
        <result property="wechatOpenid" column="wechat_openid"/>
    </resultMap>

    <sql id="selectConBcstUserConfigVo">
        select
        t.client_id,
        t.sid,
        t.dataobject_category_sid,
        t.dataobject_category_code,
        t.bcst_type,
        t.user_account,
        t.sort,
        t.remark,
        t.handle_status,
        t.creator_account,
        t.create_date,
        t.updater_account,
        t.update_date,
        t.confirmer_account,
        t.confirm_date,
        t.data_source_sys,
        t1.nick_name as creator_account_name,
        t2.name as dataobject_category_name,
        t3.nick_name as user_account_name,
        t3.user_id,
        t3.email,
        t3.work_wechat_openid,
        t3.dingtalk_openid,
        t3.wx_gzh_openid,
        t3.wx_xcx_openid,
        t3.wechat_openid
  from s_con_bcst_user_config t
        LEFT JOIN sys_user t1 ON t1.user_name = t.creator_account
        LEFT JOIN s_con_dataobject_category t2 ON t2.sid = t.dataobject_category_sid
        LEFT JOIN sys_user t3 ON t3.user_name = t.user_account
    </sql>

    <select id="selectConBcstUserConfigById" parameterType="Long" resultMap="ConBcstUserConfigResult">
        <include refid="selectConBcstUserConfigVo"/>
        where t.sid = #{sid}
    </select>


    <select id="selectConBcstUserConfigList" parameterType="com.platform.ems.plug.domain.ConBcstUserConfig"
            resultMap="ConBcstUserConfigResult">
        <include refid="selectConBcstUserConfigVo"/>
        <where>
            <if test="clientId != null  and clientId != ''">
                and (<foreach collection="clientId.split(';')" item="item" separator="or">t.client_id like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="sidList != null and sidList.length >0 ">
                and t.sid in
                (<foreach collection="sidList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="sid != null ">
                and t.sid = #{sid}
            </if>
            <if test="dataobjectCategorySid != null ">
                and t.dataobject_category_sid =#{dataobjectCategorySid}
            </if>
            <if test="dataobjectCategorySidList != null and dataobjectCategorySidList.length >0 ">
                and t.dataobject_category_sid in
                (<foreach collection="dataobjectCategorySidList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="dataobjectCategoryCode != null  and dataobjectCategoryCode != ''">
                and (<foreach collection="dataobjectCategoryCode.split(';')" item="item" separator="or">
                t.dataobject_category_code like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="bcstType != null  and bcstType != ''">
                and (<foreach collection="bcstType.split(';')" item="item" separator="or">t.bcst_type like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="bcstTypeList != null and bcstTypeList.length >0 ">
                and t.bcst_type in
                (<foreach collection="bcstTypeList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="userAccount != null  and userAccount != ''">
                and (<foreach collection="userAccount.split(';')" item="item" separator="or">t.user_account like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="userAccountList != null and userAccountList.length >0 ">
                and t.user_account in
                (<foreach collection="userAccountList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="sort != null ">
                and t.sort =#{sort}
            </if>
            <if test="handleStatus != null  and handleStatus != ''">
                and (<foreach collection="handleStatus.split(';')" item="item" separator="or">t.handle_status like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="creatorAccount != null  and creatorAccount != ''">
                and (<foreach collection="creatorAccount.split(';')" item="item" separator="or">t1.nick_name like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="beginTime != null and beginTime!=''">
                and date_format(t.create_date,'%y%m%d') &gt;= date_format(#{beginTime},'%y%m%d')
            </if>
            <if test="endTime != null and endTime!=''">
                and date_format(t.create_date,'%y%m%d') &lt;= date_format(#{endTime},'%y%m%d')
            </if>
            <if test="updaterAccount != null  and updaterAccount != ''">
                and (<foreach collection="updaterAccount.split(';')" item="item" separator="or">t.updater_account like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="updateDate != null ">
                and t.update_date = #{updateDate}
            </if>
            <if test="confirmerAccount != null  and confirmerAccount != ''">
                and (<foreach collection="confirmerAccount.split(';')" item="item" separator="or">t.confirmer_account
                like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="confirmDate != null ">
                and t.confirm_date = #{confirmDate}
            </if>
            <if test="dataSourceSys != null  and dataSourceSys != ''">
                and (<foreach collection="dataSourceSys.split(';')" item="item" separator="or">t.data_source_sys like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="params.dataScope != null and params.dataScope!=''">
                ${params.dataScope}
            </if>
        </where>
        order by t.sort asc, t2.name asc
    </select>


    <!--添加多个-->
    <insert id="inserts">
        insert into s_con_bcst_user_config
        <trim prefix="(" suffix=")" suffixOverrides=",">
            client_id,
            sid,
            dataobject_category_sid,
            dataobject_category_code,
            bcst_type,
            user_account,
            sort,
            remark,
            handle_status,
            creator_account,
            create_date,
            updater_account,
            update_date,
            confirmer_account,
            confirm_date,
            data_source_sys,
        </trim>
        values
        <foreach collection="list" item="item" separator=",">
            <trim prefix=" (" suffix=")" suffixOverrides=",">
                #{item.clientId},
                #{item.dataobjectCategorySid},
                #{item.dataobjectCategoryCode},
                #{item.bcstType},
                #{item.userAccount},
                #{item.sort},
                #{item.remark},
                #{item.handleStatus},
                #{item.creatorAccount},
                #{item.createDate},
                #{item.updaterAccount},
                #{item.updateDate},
                #{item.confirmerAccount},
                #{item.confirmDate},
                #{item.dataSourceSys},
            </trim>
        </foreach>
    </insert>

    <!--更新-->
    <update id="updateAllById">
        update s_con_bcst_user_config
        <trim prefix="SET" suffixOverrides=",">
            sid = #{sid},
            dataobject_category_sid = #{dataobjectCategorySid},
            dataobject_category_code = #{dataobjectCategoryCode},
            bcst_type = #{bcstType},
            user_account = #{userAccount},
            sort = #{sort},
            remark = #{remark},
            handle_status = #{handleStatus},
            updater_account = #{updaterAccount},
            update_date = #{updateDate},
            confirmer_account = #{confirmerAccount},
            confirm_date = #{confirmDate},
            data_source_sys = #{dataSourceSys},
        </trim>
        where sid = #{sid}
    </update>

    <!--更新多个-->
    <update id="updatesAllById">
        <foreach collection="list" item="o" separator=";">
            update s_con_bcst_user_config
            <trim prefix="SET" suffixOverrides=",">
                client_id = #{clientId},
                sid = #{sid},
                dataobject_category_sid = #{dataobjectCategorySid},
                dataobject_category_code = #{dataobjectCategoryCode},
                bcst_type = #{bcstType},
                user_account = #{userAccount},
                sort = #{sort},
                remark = #{remark},
                handle_status = #{handleStatus},
                creator_account = #{creatorAccount},
                create_date = #{createDate},
                updater_account = #{updaterAccount},
                update_date = #{updateDate},
                confirmer_account = #{confirmerAccount},
                confirm_date = #{confirmDate},
                data_source_sys = #{dataSourceSys},
            </trim>
            where sid = #{sid}
        </foreach>
    </update>

</mapper>
