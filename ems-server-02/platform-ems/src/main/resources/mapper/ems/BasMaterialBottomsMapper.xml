<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.ems.mapper.BasMaterialBottomsMapper">

    <resultMap type="com.platform.ems.domain.BasMaterialBottoms" id="BasMaterialBottomsResult">
        <result property="clientId"    column="client_id"    />
        <result property="bottomsSkuSid"    column="bottoms_sku_sid"    />
        <result property="bottomsType"    column="bottoms_type"    />
        <result property="sizePatternType"    column="size_pattern_type"    />
        <result property="skuSid"    column="sku_sid"    />
        <result property="sizeBottoms"    column="size_bottoms"    />
        <result property="remark"    column="remark"    />
        <result property="status"    column="status"    />
        <result property="handleStatus"    column="handle_status"    />
        <result property="creatorAccount"    column="creator_account"    />
        <result property="createDate"    column="create_date"    />
        <result property="updaterAccount"    column="updater_account"    />
        <result property="updateDate"    column="update_date"    />
        <result property="confirmerAccount" column="confirmer_account"/>
        <result property="confirmDate" column="confirm_date"/>
        <result property="dataSourceSys"    column="data_source_sys"    />
        <result property="companySid"    column="company_sid"    />
        <result property="confirmerAccountName" column="confirmer_account_name"/>
        <result property="updaterAccountName" column="updater_account_name"/>
        <result property="creatorAccountName" column="creator_account_name"/>
    </resultMap>

    <select id="selectBasMaterialBottomsList" parameterType="com.platform.ems.domain.BasMaterialBottoms" resultType="com.platform.ems.domain.BasMaterialBottoms">
        select
        t.bottoms_sku_sid,
        t.bottoms_type,
        t.size_pattern_type,
        t.sku_sid,
        t.company_brand_sid,
        t.size_bottoms,
        t.remark,
        t.status,
        t.handle_status,
        t.creator_account,
        t.create_date,
        t.updater_account,
        t.update_date,
        t.confirmer_account,
        t.confirm_date,
        t.company_sid,
        t1.sku_name,
        t1.sku_code,
        t1.sku_name2,
        t1.sku_name2,
        t2.company_name,
        t3.brand_name as company_brand_name,
        t4.sku_name as size_bottoms_name,
        t4.sku_code as size_bottoms_code,
        t4.sku_name2 as size_bottoms_name2,
        t5.nick_name AS creator_account_name,
        t6.nick_name AS updater_account_name,
        t7.nick_name AS confirmer_account_name
  from s_bas_material_bottoms t
        left join s_bas_sku t1 on t1.sku_sid =t.sku_sid
        left join s_bas_sku t4 on t4.sku_sid=t.size_bottoms
        left join s_bas_company t2 on t2.company_sid =t.company_sid
        left join s_bas_company_brand t3 on t3.company_brand_sid =t.company_brand_sid
        LEFT JOIN sys_user t5 ON t.creator_account = t5.user_name
        LEFT JOIN sys_user t6 ON t.updater_account = t6.user_name
        LEFT JOIN sys_user t7 ON t.confirmer_account = t7.user_name
        <where>
            <if test="skuCode != null and skuCode != ''">
                and (<foreach collection="skuCode.split(';')" item="item" separator="or"> t1.sku_code like concat('%',#{item}, '%')</foreach>)
            </if>
            <if test="skuName != null and skuName != ''">
                and (<foreach collection="skuName.split(';')" item="item" separator="or"> t1.sku_name like concat('%',#{item}, '%')</foreach>)
            </if>
            <if test="sizeBottomsCode != null and sizeBottomsCode != ''">
                and (<foreach collection="sizeBottomsCode.split(';')" item="item" separator="or"> t4.sku_code like concat('%',#{item}, '%')</foreach>)
            </if>
            <if test="sizeBottomsName != null and sizeBottomsName != ''">
                and (<foreach collection="sizeBottomsName.split(';')" item="item" separator="or"> t4.sku_name like concat('%',#{item}, '%')</foreach>)
            </if>
            <if test="bottomsType != null  and bottomsType !=''">
                and  t.bottoms_type =#{bottomsType}
            </if>
            <if test="sizePatternType != null  and sizePatternType!='' ">
                and  t.size_pattern_type =#{sizePatternType}
            </if>
            <if test="skuSids != null  and skuSids.size >0">
                and t.sku_sid in
                (<foreach collection="skuSids" item="skusid" separator=",">#{skusid}</foreach>)
            </if>
            <if test="sizeBottoms != null  and sizeBottoms != ''">
               and ( <foreach collection="sizeBottoms.split(';')" item="item" separator="or"> t.size_bottoms like concat('%', #{item}, '%')</foreach>)
            </if>
            <if test="status != null  and status !='' ">
                and  t.status =#{status}
            </if>
            <if test="handleStatus != null  and handleStatus !='' ">
                and  t.handle_status =#{handleStatus}
            </if>
            <if test="handleStatusList != null and handleStatusList.length >0 ">
                and t.handle_status in
                (<foreach collection="handleStatusList" item="handleStatus" separator=",">#{handleStatus}</foreach>)
            </if>
            <if test="creatorAccount != null  and creatorAccount != ''">
               and ( <foreach collection="creatorAccount.split(';')" item="item" separator="or"> t5.nick_name like concat('%', #{item}, '%')</foreach>)
            </if>
            <if test="createDateStart != null and createDateStart!= '' ">
                and date_format(t.create_date,'%y%m%d') &gt;= date_format(#{createDateStart},'%y%m%d')
            </if>
            <if test="createDateEnd != null and createDateEnd !=''">
                and date_format(t.create_date,'%y%m%d') &lt;=date_format(#{createDateEnd},'%y%m%d')
            </if>
        </where>
    </select>

    <!--添加多个-->
    <insert id="inserts">
        insert into s_bas_material_bottoms
        <trim prefix="(" suffix=")" suffixOverrides=",">
                          client_id,
                                  bottoms_sku_sid,
                                  bottoms_type,
                                  size_pattern_type,
                                  sku_sid,
                                  size_bottoms,
                                  remark,
                                  status,
                                  handle_status,
                                  creator_account,
                                  create_date,
                                  updater_account,
                                  update_date,
                                  data_source_sys,
                                  company_sid,
                        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
        <foreach collection="list" item="o" separator=",">
                           #{clientId},
                                   #{bottomsSkuSid},
                                   #{bottomsType},
                                   #{sizePatternType},
                                   #{skuSid},
                                   #{sizeBottoms},
                                   #{remark},
                                   #{status},
                                   #{handleStatus},
                                   #{creatorAccount},
                                   #{createDate},
                                   #{updaterAccount},
                                   now(),
                                   #{dataSourceSys},
                                   #{companySid},
                        </foreach>
        </trim>
    </insert>

    <!--更新-->
    <update id="updateAllById">
        update s_bas_material_bottoms
        <trim prefix="SET" suffixOverrides=",">
                                    bottoms_sku_sid = #{bottomsSkuSid},
                                    bottoms_type = #{bottomsType},
                                    size_pattern_type = #{sizePatternType},
                                    sku_sid = #{skuSid},
                                    size_bottoms = #{sizeBottoms},
                                    remark = #{remark},
                                    status = #{status},
                                    handle_status = #{handleStatus},
                                    creator_account = #{creatorAccount},
                                    create_date = #{createDate},
                                    updater_account = #{updaterAccount},
                                    update_date = now(),
                                    confirmer_account = #{confirmerAccount},
                                    confirm_date = now(),
                                    data_source_sys = #{dataSourceSys},
                                    company_sid = #{companySid},
                                    company_brand_sid  =#{companyBrandSid},
                        </trim>
        where bottoms_sku_sid = #{bottomsSkuSid}
    </update>

    <!--更新多个-->
    <update id="updatesAllById">
        <foreach collection="list" item="o" separator=";">
        update s_bas_material_bottoms
        <trim prefix="SET" suffixOverrides=",">

                                bottoms_type = #{bottomsType},
                                size_pattern_type = #{sizePatternType},
                                sku_sid = #{skuSid},
                                size_bottoms = #{sizeBottoms},
                                remark = #{remark},
                                status = #{status},
                                handle_status = #{handleStatus},
                                creator_account = #{creatorAccount},
                                create_date = #{createDate},
                                updater_account = #{updaterAccount},
                                update_date = #{updateDate},
                                confirmer_account = #{confirmerAccount},
                                confirm_date = now(),
                                data_source_sys = #{dataSourceSys},
                                company_sid = #{companySid},
                        </trim>
        where bottoms_sku_sid = #{bottomsSkuSid}
        </foreach>
    </update>

    <sql id="selectBasMaterialBottomsVo">
        select t.client_id, t.bottoms_sku_sid, t.bottoms_type, t.size_pattern_type, t.sku_sid, t.size_bottoms, t.remark, t.status, t.handle_status, t.creator_account, t.create_date, t.updater_account, t.update_date, t.confirmer_account, t.confirm_date, t.data_source_sys, t.company_sid,
        t2.nick_name AS creator_account_name,
        t3.nick_name AS updater_account_name,
        t4.nick_name AS confirmer_account_name
        from s_bas_material_bottoms t
        LEFT JOIN sys_user t2 ON t.creator_account = t2.user_name
        LEFT JOIN sys_user t3 ON t.updater_account = t3.user_name
        LEFT JOIN sys_user t4 ON t.confirmer_account = t4.user_name
    </sql>



</mapper>
