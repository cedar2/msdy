<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.ems.mapper.InvInventorySheetItemMapper">

    <resultMap type="com.platform.ems.domain.InvInventorySheetItem" id="InvInventorySheetItemResult">
        <result property="clientId" column="client_id"/>
        <result property="inventorySheetItemSid" column="inventory_sheet_item_sid"/>
        <result property="inventorySheetSid" column="inventory_sheet_sid"/>
        <result property="materialSid" column="material_sid"/>
        <result property="sku1Sid" column="sku1_sid"/>
        <result property="sku2Sid" column="sku2_sid"/>
        <result property="barcodeSid" column="barcode_sid"/>
        <result property="stockType" column="stock_type"/>
        <result property="stockQuantity" column="stock_quantity"/>
        <result property="countQuantity" column="count_quantity"/>
        <result property="unitBase" column="unit_base"/>
        <result property="price" column="price"/>
        <result property="batchNum" column="batch_num"/>
        <result property="reasonType" column="reason_type"/>
        <result property="itemNum" column="item_num"/>
        <result property="remark" column="remark"/>
        <result property="creatorAccount" column="creator_account"/>
        <result property="createDate" column="create_date"/>
        <result property="updaterAccount" column="updater_account"/>
        <result property="updateDate" column="update_date"/>
        <result property="dataSourceSys" column="data_source_sys"/>
    </resultMap>

    <sql id="selectInvInventorySheetItemVo">
        select t.client_id,
               t.inventory_sheet_item_sid,
               t.inventory_sheet_sid,
               t.material_sid,
               t.sku1_sid,
               t.sku2_sid,
               t.barcode_sid,
               t.stock_type,
               t.stock_quantity,
               t.count_quantity,
               t.storehouse_sid,
               ifnull(t.storehouse_code,t10.storehouse_code) as storehouse_code,
               t10.storehouse_name,
               t.storehouse_location_sid,
               ifnull(t.storehouse_location_code,t11.location_code) as storehouse_location_code,
               ifnull(t.storehouse_location_code,t11.location_code) as location_code,
               t11.location_name,
               t11.location_name as storehouse_location_name,
               t.unit_base,
               t.price,
               t.batch_num,
               t.reason_type,
               t.item_num,
               t.remark,
               t.creator_account,
               t.create_date,
               t.stock_count_result,
               t.updater_account,
               t.update_date,
               t.data_source_sys,
               t1.material_name,
               t1.material_code,
               t1.osb_sample_color,
               t1.osb_sample_size,
               t2.sku_code                              AS sku1_code,
               IFNULL(t2.sku_name, t1.osb_sample_color) AS sku1_name,
               t3.sku_code                              AS sku2_code,
               IFNULL(t3.sku_name, t1.osb_sample_size)  AS sku2_name,
               t4.name                                  as unit_base_name,
               t5.nick_name                             as creator_account_name,
               t6.barcode,
               t6.barcode2,
               t8.usage_frequency_flag,
               t9.dict_label                            as usage_frequency_flag_name
        from s_inv_inventory_sheet_item t
         LEFT JOIN s_bas_material t1 ON t1.material_sid = t.material_sid
         LEFT JOIN s_bas_sku t2 ON t2.sku_sid = t.sku1_sid
         LEFT JOIN s_bas_sku t3 ON t3.sku_sid = t.sku2_sid
         left join s_con_measure_unit t4 on t4.code = t.unit_base
         LEFT JOIN sys_user t5 ON t5.user_name = t.creator_account AND t5.client_id = t.client_id
         left join s_bas_material_barcode t6 on t6.barcode_sid = t.barcode_sid
         LEFT JOIN s_inv_storehouse_material t8
                   on t.barcode_sid = t8.barcode_sid and t.storehouse_sid = t8.storehouse_sid
         LEFT JOIN sys_dict_data t9
                   ON t9.dict_type = 'usage_frequency_flag' and t9.dict_value = t8.usage_frequency_flag
         LEFT JOIN s_bas_storehouse t10 on t.storehouse_sid = t10.storehouse_sid
         LEFT JOIN s_bas_storehouse_location t11 on t.storehouse_location_sid = t11.storehouse_location_sid
    </sql>

    <sql id="reportInvInventorySheetVo">
        select t.material_sid,
               t.sku1_sid,
               t.sku2_sid,
               t.barcode_sid,
               t.unit_base,
               t.storehouse_sid,
               t.storehouse_code,
               t.storehouse_location_sid,
               t.storehouse_location_code,
               t.stock_quantity,
               t.price,
               t.reason_type,
               t.stock_count_result,
               t.count_quantity,
               t.item_num,
               t.remark,
               t.creator_account,
               t.create_date,
               t.updater_account,
               t.update_date,
               t.data_source_sys,
               t1.material_name,
               t1.material_code,
               t2.sku_code   AS sku1_code,
               t2.sku_name   AS sku1_name,
               t3.sku_code   AS sku2_code,
               t3.sku_name   AS sku2_name,
               t4.inventory_sheet_code,
               t4.inventory_sheet_sid,
               t4.count_status,
               t4.count_result_enter_date,
               t4.plan_count_date,
               t4.accountor,
               t4.count_task_document,
               t4.refer_document,
               t4.document_date,
               t4.account_date,
               t4.special_stock,
               t4.handle_status,
               t6.name       as unit_base_name,
               t7.barcode,
               t8.storehouse_name,
               t9.location_name,
               t10.vendor_name,
               t11.customer_name,
               t12.name      as special_stock_name,
               t13.nick_name as creator_account_name,
               t14.nick_name as accountor_name,
               t15.name      as reason_type_name,
               t16.inventory_document_code,
               t17.name      as movement_type_name
        from s_inv_inventory_sheet_item t
         LEFT JOIN s_bas_material t1 ON t1.material_sid = t.material_sid
         LEFT JOIN s_bas_sku t2 ON t2.sku_sid = t.sku1_sid
         LEFT JOIN s_bas_sku t3 ON t3.sku_sid = t.sku2_sid
         left join s_inv_inventory_sheet t4 on t4.inventory_sheet_sid = t.inventory_sheet_sid
         left join s_con_measure_unit t6 on t6.code = t.unit_base
         left join s_bas_material_barcode t7 on t7.barcode_sid = t.barcode_sid
         LEFT JOIN s_bas_storehouse t8 on t8.storehouse_sid = t.storehouse_sid
         LEFT JOIN s_bas_storehouse_location t9 on t9.storehouse_location_sid = t.storehouse_location_sid
         LEFT JOIN s_bas_vendor t10 ON t10.vendor_sid = t4.vendor_sid
         LEFT JOIN s_bas_customer t11 on t11.customer_sid = t4.customer_sid
         left join s_con_special_stock t12 on t12.code = t4.special_stock
         LEFT JOIN sys_user t13 ON t.creator_account = t13.user_name AND t.client_id = t13.client_id
         LEFT JOIN sys_user t14 ON t4.accountor = t14.user_name AND t4.client_id = t14.client_id
         left join s_con_reason_type_storage t15 on t15.code = t.reason_type
         left join s_inv_inventory_document t16 on t16.refer_document_sid = t4.inventory_sheet_sid
         LEFT JOIN s_con_movement_type t17 on t17.code = t16.movement_type
    </sql>

    <select id="selectInvInventorySheetItemById" parameterType="Long" resultMap="InvInventorySheetItemResult">
        <include refid="selectInvInventorySheetItemVo"/>
        where t.inventory_sheet_sid = #{inventorySheetSid}
    </select>

    <select id="reportInvInventorySheet"
            parameterType="com.platform.ems.domain.dto.request.InvInventorySheetReportRequest"
            resultType="com.platform.ems.domain.dto.response.InvInventorySheetReportResponse">
        <include refid="reportInvInventorySheetVo"/>
        <where>
            <if test="inventorySheetCode != null and inventorySheetCode !=''">
                and t4.inventory_sheet_code like concat('%', #{inventorySheetCode}, '%')
            </if>
            <if test="materialCode != null and materialCode !=''">
                and (<foreach collection="materialCode.split(';')" item="item" separator=" or ">
                    t1.material_code like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="materialName != null and materialName !=''">
                and (<foreach collection="materialName.split(';')" item="item" separator=" or ">
                    t1.material_name like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="creatorAccount != null and creatorAccount !=''">
                and (<foreach collection="creatorAccount.split(';')" item="item" separator=" or ">
                    t13.nick_name like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="stockCountResultList != null and stockCountResultList.length >0">
                and t.stock_count_result in
                (<foreach collection="stockCountResultList" item="item" separator=",">
                #{item}</foreach>)
            </if>
            <if test="movementTypeList != null and movementTypeList.length >0">
                and t16.movement_type in
                (<foreach collection="movementTypeList" item="item" separator=",">
                #{item}</foreach>)
            </if>
            <if test="vendorSidList != null and vendorSidList.length >0">
                and t4.vendor_sid in
                (<foreach collection="vendorSidList" item="item" separator=",">
                #{item}</foreach>)
            </if>
            <if test="customerSidList != null and customerSidList.length >0">
                and t4.customer_sid in
                (<foreach collection="customerSidList" item="item" separator=",">
                #{item}</foreach>)
            </if>
            <if test="storehouseSid != null and storehouseSid != '' ">
                and t.storehouse_sid =#{storehouseSid}
            </if>
            <if test="creatorAccountList != null and creatorAccountList.length >0 ">
                and t4.creator_account in
                (<foreach collection="creatorAccountList" item="item" separator=",">
                #{item}</foreach>)
            </if>
            <if test="isFreezeStock != null and isFreezeStock != '' ">
                and t4.is_freeze_stock =#{isFreezeStock}
            </if>
            <if test="isQuantityRecord != null and isQuantityRecord != '' ">
                and t4.is_quantity_record =#{isQuantityRecord}
            </if>
            <if test="countTaskDocument != null and countTaskDocument !=''">
                and (<foreach collection="countTaskDocument.split(';')" item="item" separator=" or ">
                t4.count_task_document like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="accountor != null and accountor !=''">
                and (<foreach collection="accountor.split(';')" item="item" separator=" or ">t14.nick_name like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="countStatusList != null and countStatusList.length >0">
                and t4.count_status in
                (<foreach collection="countStatusList" item="item" separator=",">
                #{item}</foreach>)
            </if>
            <if test="storehouseLocationSidList != null and storehouseLocationSidList.length >0">
                and t.storehouse_location_sid in
                (<foreach collection="storehouseLocationSidList" item="item" separator=",">
                #{item}</foreach>)
            </if>
            <if test="accountDateBeginTime != null and accountDateBeginTime!=''">
                and date_format(t4.account_date,'%y%m%d') &gt;= date_format(#{accountDateBeginTime},'%y%m%d')
            </if>
            <if test="accountDateEndTime != null and accountDateEndTime!=''">
                and date_format(t4.account_date,'%y%m%d') &lt;= date_format(#{accountDateEndTime},'%y%m%d')
            </if>
            <if test="planCountDateBeginTime != null and planCountDateBeginTime!=''">
                and date_format(t4.plan_count_date,'%y%m%d') &gt;= date_format(#{planCountDateBeginTime},'%y%m%d')
            </if>
            <if test="planCountDateEndTime != null and planCountDateEndTime!=''">
                and date_format(t4.plan_count_date,'%y%m%d') &lt;= date_format(#{planCountDateEndTime},'%y%m%d')
            </if>
            <if test="handleStatus != null and handleStatus != ''">
                and t4.handle_status = #{handleStatus}
            </if>
            <if test="handleStatusList != null and handleStatusList.length >0">
                and t4.handle_status in
                (<foreach collection="handleStatusList" item="item" separator=",">
                #{item}</foreach>)
            </if>
            <if test="specialStockList != null and specialStockList.length >0">
                and t4.special_stock in
                (<foreach collection="specialStockList" item="item" separator=",">
                #{item}</foreach>)
            </if>
            <if test="beginTime != null and beginTime!=''">
                and date_format(t.create_date,'%y%m%d') &gt;= date_format(#{beginTime},'%y%m%d')
            </if>
            <if test="endTime != null and endTime!=''">
                and date_format(t.create_date,'%y%m%d') &lt;= date_format(#{endTime},'%y%m%d')
            </if>
        </where>
        order by t4.inventory_sheet_code desc
    </select>

    <select id="selectInvInventorySheetItemList" parameterType="com.platform.ems.domain.InvInventorySheetItem"
            resultMap="InvInventorySheetItemResult">
        <include refid="selectInvInventorySheetItemVo"/>
        <where>
            <if test="clientId != null and clientId != ''">
                and t.client_id = #{clientId}
            </if>
            <if test="inventorySheetSid != null ">
                and t.inventory_sheet_sid = #{inventorySheetSid}
            </if>
            <if test="materialSid != null ">
                and t.material_sid = #{materialSid}
            </if>
            <if test="sku1Sid != null ">
                and t.sku1_sid = #{sku1Sid}
            </if>
            <if test="sku2Sid != null ">
                and t.sku2_sid = #{sku2Sid}
            </if>
            <if test="barcodeSid != null ">
                and t.barcode_sid = #{barcodeSid}
            </if>
            <if test="stockType != null and stockType != ''">
                and (<foreach collection="stockType.split(';')" item="item" separator=" or ">
                    t.stock_type = #{item}
            </foreach>)
            </if>
            <if test="stockQuantity != null ">
                and t.stock_quantity = #{stockQuantity}
            </if>
            <if test="countQuantity != null ">
                and t.count_quantity = #{countQuantity}
            </if>
            <if test="unitBase != null and unitBase != ''">
                and (<foreach collection="unitBase.split(';')" item="item" separator=" or ">
                    t.unit_base = #{item}
            </foreach>)
            </if>
            <if test="price != null ">
                and t.price = #{price}
            </if>
            <if test="batchNum != null and batchNum != ''">
                and (<foreach collection="batchNum.split(';')" item="item" separator=" or ">
                    t.batch_num like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="reasonType != null and reasonType != ''">
                and (<foreach collection="reasonType.split(';')" item="item" separator=" or ">
                    t.reason_type = #{item}
            </foreach>)
            </if>
            <if test="itemNum != null ">
                and t.item_num = #{itemNum}
            </if>
            <if test="creatorAccount != null and creatorAccount != ''">
                and (<foreach collection="creatorAccount.split(';')" item="item" separator=" or ">
                    t.creator_account like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="beginTime != null and beginTime!=''">
                and date_format(t.create_date,'%y%m%d') &gt;= date_format(#{beginTime},'%y%m%d')
            </if>
            <if test="endTime != null and endTime!=''">
                and date_format(t.create_date,'%y%m%d') &lt;= date_format(#{endTime},'%y%m%d')
            </if>
            <if test="updaterAccount != null and updaterAccount != ''">
                and (<foreach collection="updaterAccount.split(';')" item="item" separator=" or ">
                    t.updater_account like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="updateDate != null ">
                and t.update_date = #{updateDate}
            </if>
            <if test="dataSourceSys != null and dataSourceSys != ''">
                and t.data_source_sys = #{dataSourceSys}
            </if>
        </where>
    </select>

    <!--添加多个-->
    <insert id="inserts">
        insert into s_inv_inventory_sheet_item
        <trim prefix="(" suffix=")" suffixOverrides=",">
            client_id,
            inventory_sheet_item_sid,
            inventory_sheet_sid,
            material_sid,
            sku1_sid,
            sku2_sid,
            barcode_sid,
            stock_type,
            storehouse_sid,
            storehouse_code,
            storehouse_location_sid,
            storehouse_location_code,
            stock_quantity,
            count_quantity,
            unit_base,
            price,
            batch_num,
            reason_type,
            item_num,
            remark,
            creator_account,
            create_date,
            updater_account,
            update_date,
            data_source_sys,
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <foreach collection="list" item="o" separator=",">
                #{o.clientId},
                #{o.inventorySheetItemSid},
                #{o.inventorySheetSid},
                #{o.materialSid},
                #{o.sku1Sid},
                #{o.sku2Sid},
                #{o.barcodeSid},
                #{o.stockType},
                #{o.storehouseSid},
                #{o.storehouseCode},
                #{o.storehouseLocationSid},
                #{o.storehouseLocationCode},
                #{o.stockQuantity},
                #{o.countQuantity},
                #{o.unitBase},
                #{o.price},
                #{o.batchNum},
                #{o.reasonType},
                #{o.itemNum},
                #{o.remark},
                #{o.creatorAccount},
                #{o.createDate},
                #{o.updaterAccount},
                #{o.updateDate},
                #{o.dataSourceSys},
            </foreach>
        </trim>
    </insert>

    <!--更新-->
    <update id="updateAllById">
        update s_inv_inventory_sheet_item
        <trim prefix="SET" suffixOverrides=",">
            client_id = #{clientId},
            inventory_sheet_item_sid = #{inventorySheetItemSid},
            inventory_sheet_sid = #{inventorySheetSid},
            material_sid = #{materialSid},
            sku1_sid = #{sku1Sid},
            sku2_sid = #{sku2Sid},
            barcode_sid = #{barcodeSid},
            stock_type = #{stockType},
            storehouse_sid = #{storehouseSid},
            storehouse_code = #{storehouseCode},
            storehouse_location_sid = #{storehouseLocationSid},
            storehouse_location_code = #{storehouseLocationCode},
            stock_quantity = #{stockQuantity},
            count_quantity = #{countQuantity},
            unit_base = #{unitBase},
            price = #{price},
            batch_num = #{batchNum},
            reason_type = #{reasonType},
            item_num = #{itemNum},
            remark = #{remark},
            creator_account = #{creatorAccount},
            create_date = #{createDate},
            updater_account = #{updaterAccount},
            update_date = #{updateDate},
            data_source_sys = #{dataSourceSys},
        </trim>
        where inventory_sheet_item_sid = #{inventorySheetItemSid}
    </update>

    <!--更新多个-->
    <update id="updatesAllById">
        <foreach collection="list" item="o" separator=";">
            update s_inv_inventory_sheet_item
            <trim prefix="SET" suffixOverrides=",">
                client_id = #{clientId},
                inventory_sheet_sid = #{inventorySheetSid},
                material_sid = #{materialSid},
                sku1_sid = #{sku1Sid},
                sku2_sid = #{sku2Sid},
                barcode_sid = #{barcodeSid},
                stock_type = #{stockType},
                storehouse_sid = #{storehouseSid},
                storehouse_code = #{storehouseCode},
                storehouse_location_sid = #{storehouseLocationSid},
                storehouse_location_code = #{storehouseLocationCode},
                stock_quantity = #{stockQuantity},
                count_quantity = #{countQuantity},
                unit_base = #{unitBase},
                price = #{price},
                batch_num = #{batchNum},
                reason_type = #{reasonType},
                item_num = #{itemNum},
                remark = #{remark},
                creator_account = #{creatorAccount},
                create_date = #{createDate},
                updater_account = #{updaterAccount},
                update_date = #{updateDate},
                data_source_sys = #{dataSourceSys},
            </trim>
            where inventory_sheet_item_sid = #{inventorySheetItemSid}
        </foreach>
    </update>

    <delete id="deleteInvInventorySheetItemByIds" parameterType="long">
        delete from s_inv_inventory_sheet_item where inventory_sheet_sid in
        <foreach item="inventorySheetSid" collection="array" open="(" separator="," close=")">
            #{inventorySheetSid}
        </foreach>
    </delete>
</mapper>
