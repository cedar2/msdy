<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.ems.mapper.PurOutsourcePriceInforMapper">

    <resultMap type="com.platform.ems.domain.PurOutsourcePriceInfor" id="PurOutsourcePriceInforResult">
        <result property="clientId" column="client_id"/>
        <result property="outsourcePriceInforSid" column="outsource_price_infor_sid"/>
        <result property="outsourcePriceInforCode" column="outsource_price_infor_code"/>
        <result property="vendorSid" column="vendor_sid"/>
        <result property="materialSid" column="material_sid"/>
        <result property="sku1Sid" column="sku1_sid"/>
        <result property="sku2Sid" column="sku2_sid"/>
        <result property="processSid" column="process_sid"/>
        <result property="barcodeSid" column="barcode_sid"/>
        <result property="laborTypeItemSid" column="labor_type_item_sid"/>
        <result property="materialCategory" column="material_category"/>
        <result property="priceDimension" column="price_dimension"/>
        <result property="companySid" column="company_sid"/>
        <result property="purchaseOrg" column="purchase_org"/>
        <result property="remark" column="remark"/>
        <result property="creatorAccount" column="creator_account"/>
        <result property="createDate" column="create_date"/>
        <result property="updaterAccount" column="updater_account"/>
        <result property="updateDate" column="update_date"/>
        <result property="confirmerAccount" column="confirmer_account"/>
        <result property="confirmDate" column="confirm_date"/>
        <result property="dataSourceSys" column="data_source_sys"/>
        <result property="creatorAccountName" column="creator_account_name"/>
        <result property="updaterAccountName" column="updater_account_name"/>
        <result property="confirmerAccountName" column="confirmer_account_name"/>
    </resultMap>

    <sql id="selectPurOutsourcePriceInforVo">
        select t.client_id,
               t.outsource_price_infor_sid,
               t.outsource_price_infor_code,
               t.vendor_sid,
               t.material_sid,
               t.sku1_sid,
               t.sku2_sid,
               t.process_sid,
               t.barcode_sid,
               t.labor_type_item_sid,
               t.material_category,
               t.price_dimension,
               t.company_sid,
               t.purchase_org,
               t.remark,
               t.creator_account,
               t.create_date,
               t.updater_account,
               t.update_date,
               t.confirmer_account,
               t.confirm_date,
               t.data_source_sys,
               t2.nick_name AS creator_account_name,
               t3.nick_name AS updater_account_name,
               t4.nick_name AS confirmer_account_name,
               t5.vendor_code,t5.vendor_name,t5.short_name AS vendor_short_name,
               t6.material_code,t6.material_name,
               t7.sku_code AS sku1_code,t7.sku_name AS sku1_name,
               t8.sku_code AS sku2_code,t8.sku_name AS sku2_name,
               t9.barcode,
               t10.process_code,t10.process_name,
               t11.item_code AS labor_type_item_code,t11.item_name AS labor_type_item_name,
               t12.company_code,t12.company_name,t12.short_name AS company_short_name
        from s_pur_outsource_price_infor t
         LEFT JOIN sys_user t2 ON t.creator_account = t2.user_name
         LEFT JOIN sys_user t3 ON t.updater_account = t3.user_name
         LEFT JOIN sys_user t4 ON t.confirmer_account = t4.user_name
         LEFT JOIN s_bas_vendor t5 ON t.vendor_sid = t5.vendor_sid
         LEFT JOIN s_bas_material t6 ON t.material_sid = t6.material_sid
         LEFT JOIN s_bas_sku t7 ON t.sku1_sid = t7.sku_sid
         LEFT JOIN s_bas_sku t8 ON t.sku2_sid = t8.sku_sid
         LEFT JOIN s_bas_material_barcode t9 ON t.barcode_sid = t9.barcode_sid
         LEFT JOIN s_man_process t10 ON t.process_sid = t10.process_sid
         LEFT JOIN s_con_labor_type_item t11 ON t.labor_type_item_sid = t11.labor_type_item_sid
         LEFT JOIN s_bas_company t12 ON t.company_sid = t12.company_sid
    </sql>

    <select id="selectPurOutsourcePriceInforById" parameterType="Long" resultMap="PurOutsourcePriceInforResult">
        <include refid="selectPurOutsourcePriceInforVo"/>
        where t.outsource_price_infor_sid = #{outsourcePriceInforSid}
    </select>


    <select id="selectPurOutsourcePriceInforList" parameterType="com.platform.ems.domain.PurOutsourcePriceInfor"
            resultMap="PurOutsourcePriceInforResult">
        <include refid="selectPurOutsourcePriceInforVo"/>
        <where>
            <if test="clientId != null and clientId != ''">
                and (<foreach collection="clientId.split(';')" item="item" separator="or">t.client_id = #{clientId}
            </foreach>)
            </if>
            <if test="outsourcePriceInforCode != null ">
                and t.outsource_price_infor_code =#{outsourcePriceInforCode}
            </if>
            <if test="vendorSid != null ">
                and t.vendor_sid =#{vendorSid}
            </if>
            <if test="materialSid != null ">
                and t.material_sid =#{materialSid}
            </if>
            <if test="sku1Sid != null ">
                and t.sku1_sid =#{sku1Sid}
            </if>
            <if test="sku2Sid != null ">
                and t.sku2_sid =#{sku2Sid}
            </if>
            <if test="processSid != null ">
                and t.process_sid =#{processSid}
            </if>
            <if test="barcodeSid != null ">
                and t.barcode_sid =#{barcodeSid}
            </if>
            <if test="laborTypeItemSid != null ">
                and t.labor_type_item_sid =#{laborTypeItemSid}
            </if>
            <if test="materialCategory != null and materialCategory != ''">
                and (<foreach collection="purchaseOrg.split(';')" item="item" separator="or">t.material_category = #{materialCategory}
            </foreach>)
            </if>
            <if test="priceDimension != null and priceDimension != ''">
                and (<foreach collection="purchaseOrg.split(';')" item="item" separator="or">t.price_dimension = #{priceDimension}
            </foreach>)
            </if>
            <if test="companySid != null ">
                and t.company_sid =#{companySid}
            </if>
            <if test="purchaseOrg != null and purchaseOrg != ''">
                and (<foreach collection="purchaseOrg.split(';')" item="item" separator="or">t.purchase_org = #{purchaseOrg}
            </foreach>)
            </if>
            <if test="creatorAccount != null and creatorAccount != ''">
                and (<foreach collection="creatorAccount.split(';')" item="item" separator="or">t.creator_account
                like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="beginTime != null and beginTime!=''">
                and date_format(t.create_date,'%y%m%d') &gt;= date_format(#{beginTime},'%y%m%d')
            </if>
            <if test="endTime != null and endTime!=''">
                and date_format(t.create_date,'%y%m%d') &lt;= date_format(#{endTime},'%y%m%d')
            </if>
            <if test="updaterAccount != null and updaterAccount != ''">
                and (<foreach collection="updaterAccount.split(';')" item="item" separator="or">t.updater_account
                like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="updateDate != null ">
                and t.update_date = #{updateDate}
            </if>
            <if test="confirmerAccount != null and confirmerAccount != ''">
                and (<foreach collection="confirmerAccount.split(';')" item="item" separator="or">t.confirmer_account
                like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="confirmDate != null ">
                and t.confirm_date = #{confirmDate}
            </if>
            <if test="dataSourceSys != null and dataSourceSys != ''">
                and (<foreach collection="dataSourceSys.split(';')" item="item" separator="or">t.data_source_sys
                like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="params.dataScope != null and params.dataScope!=''">
                ${params.dataScope}
            </if>
        </where>
    </select>


    <!--添加多个-->
    <insert id="inserts">
        insert into s_pur_outsource_price_infor
        <trim prefix="(" suffix=")" suffixOverrides=",">
            client_id,
            outsource_price_infor_sid,
            outsource_price_infor_code,
            vendor_sid,
            material_sid,
            sku1_sid,
            sku2_sid,
            process_sid,
            barcode_sid,
            labor_type_item_sid,
            material_category,
            price_dimension,
            company_sid,
            purchase_org,
            remark,
            creator_account,
            create_date,
            updater_account,
            update_date,
            confirmer_account,
            confirm_date,
            data_source_sys,
        </trim>
        values
        <foreach collection="list" item="item" separator=",">
            <trim prefix=" (" suffix=")" suffixOverrides=",">
                #{item.clientId},
                #{item.outsourcePriceInforSid},
                #{item.outsourcePriceInforCode},
                #{item.vendorSid},
                #{item.materialSid},
                #{item.sku1Sid},
                #{item.sku2Sid},
                #{item.processSid},
                #{item.barcodeSid},
                #{item.laborTypeItemSid},
                #{item.materialCategory},
                #{item.priceDimension},
                #{item.companySid},
                #{item.purchaseOrg},
                #{item.remark},
                #{item.creatorAccount},
                #{item.createDate},
                #{item.updaterAccount},
                #{item.updateDate},
                #{item.confirmerAccount},
                #{item.confirmDate},
                #{item.dataSourceSys},
            </trim>
        </foreach>
    </insert>

    <!--更新-->
    <update id="updateAllById">
        update s_pur_outsource_price_infor
        <trim prefix="SET" suffixOverrides=",">
            vendor_sid = #{vendorSid},
            material_sid = #{materialSid},
            sku1_sid = #{sku1Sid},
            sku2_sid = #{sku2Sid},
            process_sid = #{processSid},
            barcode_sid = #{barcodeSid},
            labor_type_item_sid = #{laborTypeItemSid},
            material_category = #{materialCategory},
            price_dimension = #{priceDimension},
            company_sid = #{companySid},
            purchase_org = #{purchaseOrg},
            remark = #{remark},
            updater_account = #{updaterAccount},
            update_date = #{updateDate},
            confirmer_account = #{confirmerAccount},
            confirm_date = #{confirmDate},
            data_source_sys = #{dataSourceSys},
        </trim>
        where outsource_price_infor_sid = #{outsourcePriceInforSid}
    </update>

    <!--更新多个-->
    <update id="updatesAllById">
        <foreach collection="list" item="o" separator=";">
            update s_pur_outsource_price_infor
            <trim prefix="SET" suffixOverrides=",">
                vendor_sid = #{vendorSid},
                material_sid = #{materialSid},
                sku1_sid = #{sku1Sid},
                sku2_sid = #{sku2Sid},
                process_sid = #{processSid},
                barcode_sid = #{barcodeSid},
                labor_type_item_sid = #{laborTypeItemSid},
                material_category = #{materialCategory},
                price_dimension = #{priceDimension},
                company_sid = #{companySid},
                purchase_org = #{purchaseOrg},
                remark = #{remark},
                updater_account = #{updaterAccount},
                update_date = #{updateDate},
                confirmer_account = #{confirmerAccount},
                confirm_date = #{confirmDate},
                data_source_sys = #{dataSourceSys},
            </trim>
            where outsource_price_infor_sid = #{outsourcePriceInforSid}
        </foreach>
    </update>

</mapper>
