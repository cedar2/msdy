<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.ems.mapper.FinCustomerInvoiceRecordAttachmentMapper">

    <resultMap type="com.platform.ems.domain.FinCustomerInvoiceRecordAttachment"
               id="FinCustomerInvoiceRecordAttachmentResult">
        <result property="clientId" column="client_id"/>
        <result property="customerInvoiceRecordAttachmentSid" column="customer_invoice_record_attachment_sid"/>
        <result property="customerInvoiceRecordSid" column="customer_invoice_record_sid"/>
        <result property="fileType" column="file_type"/>
        <result property="fileName" column="file_name"/>
        <result property="filePath" column="file_path"/>
        <result property="remark" column="remark"/>
        <result property="creatorAccount" column="creator_account"/>
        <result property="createDate" column="create_date"/>
        <result property="updaterAccount" column="updater_account"/>
        <result property="updateDate" column="update_date"/>
        <result property="dataSourceSys" column="data_source_sys"/>
        <result property="creatorAccountName" column="creator_account_name"/>
        <result property="updaterAccountName" column="updater_account_name"/>
    </resultMap>

    <sql id="selectFinCustomerInvoiceRecordAttachmentVo">
        select t.client_id,
               t.customer_invoice_record_attachment_sid,
               t.customer_invoice_record_sid,
               t.file_type,
               t.file_name,
               t.file_path,
               t.remark,
               t.creator_account,
               t.create_date,
               t.updater_account,
               t.update_date,
               t.data_source_sys,
               u1.nick_name AS creator_account_name,
               u2.nick_name AS updater_account_name
        from s_fin_customer_invoice_record_attachment t
                 LEFT JOIN sys_user u1 ON t.creator_account = u1.user_name
                 LEFT JOIN sys_user u2 ON t.updater_account = u2.user_name
    </sql>

    <select id="selectFinCustomerInvoiceRecordAttachmentById" parameterType="Long"
            resultMap="FinCustomerInvoiceRecordAttachmentResult">
        <include refid="selectFinCustomerInvoiceRecordAttachmentVo"/>
        where t.customer_invoice_record_attachment_sid = #{customerInvoiceRecordAttachmentSid}
    </select>

    <select id="selectFinCustomerInvoiceRecordAttachmentList"
            parameterType="com.platform.ems.domain.FinCustomerInvoiceRecordAttachment"
            resultMap="FinCustomerInvoiceRecordAttachmentResult">
        <include refid="selectFinCustomerInvoiceRecordAttachmentVo"/>
        <where>
            <if test="clientId != null and clientId != ''">
                and t.client_id = #{clientId}
            </if>
            <if test="customerInvoiceRecordSid != null ">
                and t.customer_invoice_record_sid = #{customerInvoiceRecordSid}
            </if>
            <if test="fileType != null and fileType != ''">
                and t.file_type = #{fileType}
            </if>
            <if test="fileName != null and fileName != ''">and
                t.file_name like concat('%', #{fileName}, '%')
            </if>
            <if test="filePath != null and filePath != ''">
                and t.file_path = #{filePath}
            </if>
            <if test="creatorAccount != null and creatorAccount != ''">
                and t.creator_account = #{creatorAccount}
            </if>
            <if test="beginTime != null and beginTime!=''">
                and date_format(t.create_date,'%y%m%d') &gt;= date_format(#{beginTime},'%y%m%d')
            </if>
            <if test="endTime != null and endTime!=''">
                and date_format(t.create_date,'%y%m%d') &lt;= date_format(#{endTime},'%y%m%d')
            </if>
            <if test="updaterAccount != null and updaterAccount != ''">
                and t.updater_account = #{updaterAccount}
            </if>
            <if test="updateDate != null ">
                and t.update_date = #{updateDate}
            </if>
            <if test="dataSourceSys != null and dataSourceSys != ''">
                and t.data_source_sys = #{dataSourceSys}
            </if>
            <if test="params.dataScope != null and params.dataScope!=''">
                ${params.dataScope}
            </if>
        </where>
    </select>

    <!--添加多个-->
    <insert id="inserts">
        insert into s_fin_customer_invoice_record_attachment
        <trim prefix="(" suffix=")" suffixOverrides=",">
            client_id,
            customer_invoice_record_attachment_sid,
            customer_invoice_record_sid,
            file_type,
            file_name,
            file_path,
            remark,
            creator_account,
            create_date,
            data_source_sys,
        </trim>
        values
        <foreach collection="list" item="item" separator=",">
            <trim prefix=" (" suffix=")" suffixOverrides=",">
                #{item.clientId},
                #{item.customerInvoiceRecordAttachmentSid},
                #{item.customerInvoiceRecordSid},
                #{item.fileType},
                #{item.fileName},
                #{item.filePath},
                #{item.remark},
                #{item.creatorAccount},
                #{item.createDate},
                #{item.dataSourceSys},
            </trim>
        </foreach>
    </insert>

    <!--更新-->
    <update id="updateAllById">
        update s_fin_customer_invoice_record_attachment
        <trim prefix="SET" suffixOverrides=",">
            file_type = #{fileType},
            file_name = #{fileName},
            file_path = #{filePath},
            remark = #{remark},
            updater_account = #{updaterAccount},
            update_date = #{updateDate},
        </trim>
        where customer_invoice_record_attachment_sid = #{customerInvoiceRecordAttachmentSid}
    </update>

    <!--更新多个-->
    <update id="updatesAllById">
        <foreach collection="list" item="o" separator=";">
            update s_fin_customer_invoice_record_attachment
            <trim prefix="SET" suffixOverrides=",">
                file_type = #{o.fileType},
                file_name = #{o.fileName},
                file_path = #{o.filePath},
                remark = #{o.remark},
                updater_account = #{o.updaterAccount},
                update_date = #{o.updateDate},
            </trim>
            where customer_invoice_record_attachment_sid = #{o.customerInvoiceRecordAttachmentSid}
        </foreach>
    </update>

</mapper>
