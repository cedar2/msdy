<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.ems.mapper.BasVendorSupplierMapper">

    <resultMap type="com.platform.ems.domain.BasVendorSupplier" id="BasVendorSupplierResult">
        <result property="clientId" column="client_id"/>
        <result property="vendorSupplierSid" column="vendor_supplier_sid"/>
        <result property="vendorSid" column="vendor_sid"/>
        <result property="supplierName" column="supplier_name"/>
        <result property="purchaseClass" column="purchase_class"/>
        <result property="remark" column="remark"/>
        <result property="creatorAccount" column="creator_account"/>
        <result property="createDate" column="create_date"/>
        <result property="updaterAccount" column="updater_account"/>
        <result property="updateDate" column="update_date"/>
        <result property="dataSourceSys" column="data_source_sys"/>
        <result property="creatorAccountName" column="creator_account_name"/>
        <result property="updaterAccountName" column="updater_account_name"/>
    </resultMap>

    <sql id="selectBasVendorSupplierVo">
        select t.client_id,
               t.vendor_supplier_sid,
               t.vendor_sid,
               t.supplier_name,
               t.purchase_class,
               t.remark,
               t.creator_account,
               t.create_date,
               t.updater_account,
               t.update_date,
               t.data_source_sys,
               t2.nick_name AS creator_account_name,
               t3.nick_name AS updater_account_name
        from s_bas_vendor_supplier t
        left join sys_user t2 on t.creator_account = t2.user_name
        left join sys_user t3 on t.updater_account = t3.user_name
    </sql>

    <select id="selectBasVendorSupplierById" parameterType="Long" resultMap="BasVendorSupplierResult">
        <include refid="selectBasVendorSupplierVo"/>
        where t.vendor_supplier_sid = #{vendorSupplierSid}
    </select>


    <select id="selectBasVendorSupplierList" parameterType="com.platform.ems.domain.BasVendorSupplier"
            resultMap="BasVendorSupplierResult">
        <include refid="selectBasVendorSupplierVo"/>
        <where>
            <if test="clientId != null  and clientId != ''">
                and (<foreach collection="clientId.split(';')" item="item" separator="or">t.client_id like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="vendorSid != null ">
                and t.vendor_sid =#{vendorSid}
            </if>
            <if test="supplierName != null  and supplierName != ''">and
                t.supplier_name like concat('%', #{supplierName}, '%')
            </if>
            <if test="purchaseClass != null  and purchaseClass != ''">
                and (<foreach collection="purchaseClass.split(';')" item="item" separator="or">t.purchase_class like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="creatorAccount != null  and creatorAccount != ''">
                and (<foreach collection="creatorAccount.split(';')" item="item" separator="or">t.creator_account like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="beginTime != null and beginTime!=''">
                and date_format(t.create_date,'%y%m%d') &gt;= date_format(#{beginTime},'%y%m%d')
            </if>
            <if test="endTime != null and endTime!=''">
                and date_format(t.create_date,'%y%m%d') &lt;= date_format(#{endTime},'%y%m%d')
            </if>
            <if test="updaterAccount != null  and updaterAccount != ''">
                and (<foreach collection="updaterAccount.split(';')" item="item" separator="or">t.updater_account like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="updateDate != null ">
                and t.update_date = #{updateDate}
            </if>
            <if test="dataSourceSys != null  and dataSourceSys != ''">
                and (<foreach collection="dataSourceSys.split(';')" item="item" separator="or">t.data_source_sys like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="params.dataScope != null and params.dataScope!=''">
                ${params.dataScope}
            </if>
        </where>
    </select>


    <!--添加多个-->
    <insert id="inserts">
        insert into s_bas_vendor_supplier
        <trim prefix="(" suffix=")" suffixOverrides=",">
            client_id,
            vendor_supplier_sid,
            vendor_sid,
            supplier_name,
            purchase_class,
            remark,
            creator_account,
            create_date,
            updater_account,
            update_date,
            data_source_sys,
        </trim>
        values
        <foreach collection="list" item="item" separator=",">
            <trim prefix=" (" suffix=")" suffixOverrides=",">
                #{item.clientId},
                #{item.vendorSupplierSid},
                #{item.vendorSid},
                #{item.supplierName},
                #{item.purchaseClass},
                #{item.remark},
                #{item.creatorAccount},
                #{item.createDate},
                #{item.updaterAccount},
                #{item.updateDate},
                #{item.dataSourceSys},
            </trim>
        </foreach>
    </insert>

    <!--更新-->
    <update id="updateAllById">
        update s_bas_vendor_supplier
        <trim prefix="SET" suffixOverrides=",">
            client_id = #{clientId},
            vendor_supplier_sid = #{vendorSupplierSid},
            vendor_sid = #{vendorSid},
            supplier_name = #{supplierName},
            purchase_class = #{purchaseClass},
            remark = #{remark},
            creator_account = #{creatorAccount},
            create_date = #{createDate},
            updater_account = #{updaterAccount},
            update_date = #{updateDate},
            data_source_sys = #{dataSourceSys},
        </trim>
        where vendor_supplier_sid = #{vendorSupplierSid}
    </update>

    <!--更新多个-->
    <update id="updatesAllById">
        <foreach collection="list" item="o" separator=";">
            update s_bas_vendor_supplier
            <trim prefix="SET" suffixOverrides=",">
                client_id = #{clientId},
                vendor_supplier_sid = #{vendorSupplierSid},
                vendor_sid = #{vendorSid},
                supplier_name = #{supplierName},
                purchase_class = #{purchaseClass},
                remark = #{remark},
                creator_account = #{creatorAccount},
                create_date = #{createDate},
                updater_account = #{updaterAccount},
                update_date = #{updateDate},
                data_source_sys = #{dataSourceSys},
            </trim>
            where vendor_supplier_sid = #{vendorSupplierSid}
        </foreach>
    </update>

</mapper>
