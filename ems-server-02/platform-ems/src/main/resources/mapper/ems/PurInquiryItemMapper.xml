<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.ems.mapper.PurInquiryItemMapper">

    <resultMap type="com.platform.ems.domain.PurInquiryItem" id="PurInquiryItemResult">
        <result property="clientId" column="client_id"/>
        <result property="inquiryItemSid" column="inquiry_item_sid"/>
        <result property="inquirySid" column="inquiry_sid"/>
        <result property="materialSid" column="material_sid"/>
        <result property="sku1Sid" column="sku1_sid"/>
        <result property="sku2Sid" column="sku2_sid"/>
        <result property="barcodeSid" column="barcode_sid"/>
        <result property="priceDimension" column="price_dimension"/>
        <result property="priceEnterMode" column="price_enter_mode"/>
        <result property="unitBase" column="unit_base"/>
        <result property="itemNum" column="item_num"/>
        <result property="remark" column="remark"/>
        <result property="creatorAccount" column="creator_account"/>
        <result property="createDate" column="create_date"/>
        <result property="updaterAccount" column="updater_account"/>
        <result property="updateDate" column="update_date"/>
        <result property="dataSourceSys" column="data_source_sys"/>
        <result property="creatorAccountName" column="creator_account_name"/>
        <result property="updaterAccountName" column="updater_account_name"/>
        <result property="sku1Name" column="sku1_name"/>
        <result property="sku1Code" column="sku1_code"/>
        <result property="sku1Type" column="sku1_type"/>
        <result property="sku2Code" column="sku2_code"/>
        <result property="sku2Name" column="sku2_name"/>
        <result property="sku2Type" column="sku2_type"/>
        <result property="sku2Type" column="sku2_type"/>
        <result property="sku2Type" column="sku2_type"/>
        <result property="materialCode" column="material_code"/>
        <result property="materialName" column="material_name"/>
        <result property="width" column="width"/>
        <result property="gramWeight" column="gram_weight"/>
        <result property="composition" column="composition"/>
        <result property="specificationSize" column="specification_size"/>
        <result property="modelSize" column="model_size"/>
        <result property="materialComposition" column="material_composition"/>
        <result property="unitBaseName" column="unit_base_name"/>
        <result property="materialTypeName" column="material_type_name"/>
        <result property="materialClassName" column="material_class_name"/>
        <result property="inquiryCode" column="inquiry_code"/>
        <result property="buyerName" column="buyer_name"/>
        <result property="purchaseGroupName" column="purchase_group_name"/>
        <result property="purchaseOrgName" column="purchase_org_name"/>
        <result property="vendorName" column="vendor_name"/>
        <result property="companyName" column="company_name"/>
        <result property="productSeasonName" column="product_season_name"/>
        <result property="rawMaterialMode" column="raw_material_mode"/>
        <result property="purchaseMode" column="purchase_mode"/>
        <result property="buyerTelephone" column="buyer_telephone"/>
        <result property="buyerEmail" column="buyer_email"/>
        <result property="currency" column="currency"/>
        <result property="currencyUnit" column="currency_unit"/>
        <result property="materialSkuSid" column="material_sku_sid"/>
        <result property="isQuoteProcess" column="is_quote_process"/>
        <result property="dateQuote" column="date_quote"/>
        <result property="quoteBargainCode" column="quote_bargain_code"/>
    </resultMap>

    <sql id="selectPurInquiryItemVo">
        select t.client_id,
               t.inquiry_item_sid,
               t.inquiry_sid,
               t.material_sid,
               t.sku1_sid,
               t.sku2_sid,
               t.barcode_sid,
               t.price_dimension,
               t.price_enter_mode,
               t.unit_base,
               t.item_num,
               t.remark,
               t.creator_account,
               t.create_date,
               t.updater_account,
               t.update_date,
               t.data_source_sys,
               t2.nick_name AS creator_account_name,
               t3.nick_name AS updater_account_name,
               t4.material_code,
               t4.material_name,
               t4.gram_weight,
               t4.composition,
               t4.specification_size,
               t4.width,
               t4.model_size,
               t4.material_composition,
               t4.material_type,
               t4.material_class_sid,
               t5.sku_name  AS sku1_name,
               t5.sku_code  AS sku1_code,
               t5.sku_type  AS sku1_type,
               t6.sku_name  AS sku2_name,
               t6.sku_code  AS sku2_code,
               t6.sku_type  AS sku2_type,
               t7.name      AS unit_base_name,
               t8.node_name AS material_class_name,
               t9.name      AS material_type_name,
               t10.inquiry_code,
               t11.material_sku_sid
        from s_pur_inquiry_item t
                 left join sys_user t2 on t.creator_account = t2.user_name
                 left join sys_user t3 on t.updater_account = t3.user_name
                 left join s_bas_material t4 on t.material_sid = t4.material_sid
                 left join s_bas_sku t5 on t.sku1_sid = t5.sku_sid
                 left join s_bas_sku t6 on t.sku2_sid = t6.sku_sid
                 LEFT JOIN s_con_measure_unit t7 ON t.unit_base = t7.code
                 LEFT JOIN s_con_material_class t8 ON t4.material_class_sid = t8.material_class_sid
                 LEFT JOIN s_con_material_type t9 ON t4.material_type = t9.code
                 left join s_pur_inquiry t10 on t.inquiry_sid = t10.inquiry_sid
                 left join s_bas_material_sku t11 on t.material_sid = t11.material_sid and t.sku1_sid = t11.sku_sid
    </sql>

    <select id="selectPurInquiryItemById" parameterType="Long" resultMap="PurInquiryItemResult">
        <include refid="selectPurInquiryItemVo"/>
        where t.inquiry_item_sid = #{inquiryItemSid}
    </select>

    <select id="selectPurInquiryItemListById" parameterType="Long" resultMap="PurInquiryItemResult">
        <include refid="selectPurInquiryItemVo"/>
        where t.inquiry_sid = #{inquirySid}
    </select>

    <delete id="deletePurInquiryItemByInquirySids">
        delete
        from s_pur_inquiry_item t
        where t.inquiry_sid in (<foreach collection="inquirySid" item="sid" separator=",">#{sid}</foreach>)
    </delete>

    <select id="selectPurInquiryItemSidListById" resultType="Long">
        select t.inquiry_item_sid
        from s_pur_inquiry_item t
        where t.inquiry_sid in (<foreach collection="inquirySid" item="sid" separator=",">#{sid}</foreach>)
    </select>

    <select id="selectPurInquiryItemList" parameterType="com.platform.ems.domain.PurInquiryItem"
            resultMap="PurInquiryItemResult">
        <include refid="selectPurInquiryItemVo"/>
        <where>
            <if test="clientId != null  and clientId != ''">
                and (<foreach collection="clientId.split(';')" item="item" separator="or">t.client_id like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="inquirySid != null ">
                and t.inquiry_sid =#{inquirySid}
            </if>
            <if test="materialSid != null ">
                and t.material_sid =#{materialSid}
            </if>
            <if test="sku1Sid != null ">
                and t.sku1_sid =#{sku1Sid}
            </if>
            <if test="sku2Sid != null ">
                and t.sku2_sid =#{sku2Sid}
            </if>
            <if test="barcodeSid != null ">
                and t.barcode_sid =#{barcodeSid}
            </if>
            <if test="priceDimension != null ">
                and t.price_dimension =#{priceDimension}
            </if>
            <if test="priceEnterMode != null  and priceEnterMode != ''">
                and (<foreach collection="priceEnterMode.split(';')" item="item" separator="or">t.price_enter_mode like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="unitBase != null  and unitBase != ''">
                and (<foreach collection="unitBase.split(';')" item="item" separator="or">t.unit_base like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="itemNum != null ">
                and t.item_num =#{itemNum}
            </if>
            <if test="creatorAccount != null  and creatorAccount != ''">
                and (<foreach collection="creatorAccount.split(';')" item="item" separator="or">t.creator_account like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="creatorAccountName != null  and creatorAccountName != ''">
                and (<foreach collection="creatorAccountName.split(';')" item="item" separator="or">t2.nick_name like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="beginTime != null and beginTime!=''">
                and date_format(t.create_date,'%y%m%d') &gt;= date_format(#{beginTime},'%y%m%d')
            </if>
            <if test="endTime != null and endTime!=''">
                and date_format(t.create_date,'%y%m%d') &lt;= date_format(#{endTime},'%y%m%d')
            </if>
            <if test="updaterAccount != null  and updaterAccount != ''">
                and (<foreach collection="updaterAccount.split(';')" item="item" separator="or">t.updater_account like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="updateDate != null ">
                and t.update_date = #{updateDate}
            </if>
            <if test="dataSourceSys != null  and dataSourceSys != ''">
                and (<foreach collection="dataSourceSys.split(';')" item="item" separator="or">t.data_source_sys like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="params.dataScope != null and params.dataScope!=''">
                ${params.dataScope}
            </if>
        </where>
    </select>


    <!--添加多个-->
    <insert id="inserts">
        insert into s_pur_inquiry_item
        <trim prefix="(" suffix=")" suffixOverrides=",">
            client_id,
            inquiry_item_sid,
            inquiry_sid,
            material_sid,
            sku1_sid,
            sku2_sid,
            barcode_sid,
            price_dimension,
            price_enter_mode,
            unit_base,
            item_num,
            remark,
            creator_account,
            create_date,
            updater_account,
            update_date,
            data_source_sys,
        </trim>
        values
        <foreach collection="list" item="item" separator=",">
            <trim prefix=" (" suffix=")" suffixOverrides=",">
                #{item.clientId},
                #{item.inquiryItemSid},
                #{item.inquirySid},
                #{item.materialSid},
                #{item.sku1Sid},
                #{item.sku2Sid},
                #{item.barcodeSid},
                #{item.priceDimension},
                #{item.priceEnterMode},
                #{item.unitBase},
                #{item.itemNum},
                #{item.remark},
                #{item.creatorAccount},
                #{item.createDate},
                #{item.updaterAccount},
                #{item.updateDate},
                #{item.dataSourceSys},
            </trim>
        </foreach>
    </insert>

    <!--更新-->
    <update id="updateAllById">
        update s_pur_inquiry_item
        <trim prefix="SET" suffixOverrides=",">
            inquiry_sid = #{inquirySid},
            material_sid = #{materialSid},
            sku1_sid = #{sku1Sid},
            sku2_sid = #{sku2Sid},
            barcode_sid = #{barcodeSid},
            price_dimension = #{priceDimension},
            price_enter_mode = #{priceEnterMode},
            unit_base = #{unitBase},
            item_num = #{itemNum},
            remark = #{remark},
            updater_account = #{updaterAccount},
            update_date = #{updateDate},
            data_source_sys = #{dataSourceSys},
        </trim>
        where inquiry_item_sid = #{inquiryItemSid}
    </update>

    <!--更新多个-->
    <update id="updatesAllById">
        <foreach collection="list" item="o" separator=";">
            update s_pur_inquiry_item
            <trim prefix="SET" suffixOverrides=",">
                inquiry_sid = #{inquirySid},
                material_sid = #{materialSid},
                sku1_sid = #{sku1Sid},
                sku2_sid = #{sku2Sid},
                barcode_sid = #{barcodeSid},
                price_dimension = #{priceDimension},
                price_enter_mode = #{priceEnterMode},
                unit_base = #{unitBase},
                item_num = #{itemNum},
                remark = #{remark},
                updater_account = #{updaterAccount},
                update_date = #{updateDate},
                data_source_sys = #{dataSourceSys},
            </trim>
            where inquiry_item_sid = #{inquiryItemSid}
        </foreach>
    </update>

    <sql id="selectPurInquiryItemReport">
        select t.client_id,
               t.inquiry_item_sid,
               t.inquiry_sid,
               t.material_sid,
               t.sku1_sid,
               t.sku2_sid,
               t.barcode_sid,
               t.price_dimension,
               t.price_enter_mode,
               t.unit_base,
               t.item_num,
               t.remark,
               t.creator_account,
               t.create_date,
               t.updater_account,
               t.update_date,
               t.data_source_sys,
               t1.inquiry_code,
               t1.buyer_telephone,
               t1.buyer_email,
               t1.quoteprice_deadline,
               t1.raw_material_mode,
               t1.purchase_mode,
               t1.date_request,
               t1.currency,
               t1.currency_unit,
               t1.remark AS inquiry_remark,
               t2.nick_name AS creator_account_name,
               t3.nick_name AS updater_account_name,
               t4.material_code,
               t4.material_name,
               t4.gram_weight,
               t4.composition,
               t4.specification_size,
               t4.width,
               t4.model_size,
               t4.material_composition,
               t4.material_type,
               t4.material_class_sid,
               t5.sku_name  AS sku1_name,
               t5.sku_code  AS sku1_code,
               t5.sku_type  AS sku1_type,
               t6.sku_name  AS sku2_name,
               t6.sku_code  AS sku2_code,
               t6.sku_type  AS sku2_type,
               t7.name      AS unit_base_name,
               t8.node_name AS material_class_name,
               t9.name      AS material_type_name,
               t10.nick_name AS buyer_name,
               t12.company_name,
               t13.product_season_name,
               t14.name AS purchase_group_name,
               t15.name AS purchase_org_name
        from s_pur_inquiry_item t
                 left join s_pur_inquiry t1 on t.inquiry_sid = t1.inquiry_sid
                 left join sys_user t2 on t.creator_account = t2.user_name
                 left join sys_user t3 on t.updater_account = t3.user_name
                 left join s_bas_material t4 on t.material_sid = t4.material_sid
                 left join s_bas_sku t5 on t.sku1_sid = t5.sku_sid
                 left join s_bas_sku t6 on t.sku2_sid = t6.sku_sid
                 left join s_con_measure_unit t7 on t.unit_base = t7.code
                 left join s_con_material_class t8 on t4.material_class_sid = t8.material_class_sid
                 left join s_con_material_type t9 on t4.material_type = t9.code
                 left join sys_user t10 on t1.buyer = t10.user_name
                 left join s_bas_company t12 on t1.company_sid = t12.company_sid
                 left join s_bas_product_season t13 on t1.product_season_sid = t13.product_season_sid
                 left join s_con_purchase_group t14 on t1.purchase_group = t14.code
                 left join s_con_purchase_org t15 on t1.purchase_org = t15.code
    </sql>

    <select id="getReportForm" parameterType="com.platform.ems.domain.PurInquiryItem"
            resultMap="PurInquiryItemResult">
        <include refid="selectPurInquiryItemReport"/>
        <where>
            <if test="inquirySid != null ">
                and t.inquiry_sid =#{inquirySid}
            </if>
            <if test="inquiryCode != null ">
                and t1.inquiry_code like concat('%', #{inquiryCode}, '%')
            </if>
            <if test="rawMaterialMode != null  and rawMaterialMode != ''">
                and t1.raw_material_mode = #{rawMaterialMode}
            </if>
            <if test="materialCode != null  and materialCode != ''">
                and (<foreach collection="materialCode.split(';')" item="item" separator="or">t4.material_code like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="materialName != null  and materialName != ''">
                and (<foreach collection="materialName.split(';')" item="item" separator="or">t4.material_name like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="quotepriceDeadlineBegin != null and quotepriceDeadlineBegin!=''">
                and date_format(t1.quoteprice_deadline,'%y%m%d') &gt;= date_format(#{quotepriceDeadlineBegin},'%y%m%d')
            </if>
            <if test="quotepriceDeadlineEnd != null and quotepriceDeadlineEnd!=''">
                and date_format(t1.quoteprice_deadline,'%y%m%d') &lt;= date_format(#{quotepriceDeadlineEnd},'%y%m%d')
            </if>
            <if test="buyerName != null  and buyerName != ''">
                and (<foreach collection="buyerName.split(';')" item="item" separator="or">t10.nick_name like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="purchaseOrgList != null and purchaseOrgList.length >0">and t1.purchase_org in
                (<foreach collection="purchaseOrgList" item="purchaseOrg" separator=",">#{purchaseOrg}</foreach>)
            </if>
            <if test="purchaseGroupList != null and purchaseGroupList.length >0">and t1.purchase_group in
                (<foreach collection="purchaseGroupList" item="purchaseGroup" separator=",">#{purchaseGroup}</foreach>)
            </if>
            <if test="companySidList != null and companySidList.length >0">and t1.company_sid in
                (<foreach collection="companySidList" item="companySid" separator=",">#{companySid}</foreach>)
            </if>
            <if test="productSeasonSidList != null and productSeasonSidList.length >0">and t1.product_season_sid in
                (<foreach collection="productSeasonSidList" item="productSeasonSid" separator=",">#{productSeasonSid}</foreach>)
            </if>
            <if test="clientId != null  and clientId != ''">
                and (<foreach collection="clientId.split(';')" item="item" separator="or">t.client_id like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="priceDimension != null ">
                and t.price_dimension =#{priceDimension}
            </if>
            <if test="priceEnterMode != null  and priceEnterMode != ''">
                and (<foreach collection="priceEnterMode.split(';')" item="item" separator="or">t.price_enter_mode like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="creatorAccount != null  and creatorAccount != ''">
                and (<foreach collection="creatorAccount.split(';')" item="item" separator="or">t.creator_account like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="creatorAccountName != null  and creatorAccountName != ''">
                and (<foreach collection="creatorAccountName.split(';')" item="item" separator="or">t2.nick_name like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="beginTime != null and beginTime!=''">
                and date_format(t.create_date,'%y%m%d') &gt;= date_format(#{beginTime},'%y%m%d')
            </if>
            <if test="endTime != null and endTime!=''">
                and date_format(t.create_date,'%y%m%d') &lt;= date_format(#{endTime},'%y%m%d')
            </if>
            <if test="dataSourceSys != null  and dataSourceSys != ''">
                and (<foreach collection="dataSourceSys.split(';')" item="item" separator="or">t.data_source_sys like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="params.dataScope != null and params.dataScope!=''">
                ${params.dataScope}
            </if>
        </where>
    </select>

</mapper>
