<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.ems.mapper.InvInventoryAdjustItemMapper">

    <resultMap type="com.platform.ems.domain.InvInventoryAdjustItem" id="InvInventoryAdjustItemResult">
        <result property="clientId" column="client_id"/>
        <result property="inventoryAdjustItemSid" column="inventory_adjust_item_sid"/>
        <result property="inventoryAdjustSid" column="inventory_adjust_sid"/>
        <result property="materialSid" column="material_sid"/>
        <result property="sku1Sid" column="sku1_sid"/>
        <result property="sku2Sid" column="sku2_sid"/>
        <result property="barcodeSid" column="barcode_sid"/>
        <result property="unitBase" column="unit_base"/>
        <result property="quantity" column="quantity"/>
        <result property="storehouseSid" column="storehouse_sid"/>
        <result property="storehouseLocationSid" column="storehouse_location_sid"/>
        <result property="reasonType" column="reason_type"/>
        <result property="movementType" column="movement_type"/>
        <result property="itemNum" column="item_num"/>
        <result property="remark" column="remark"/>
        <result property="creatorAccount" column="creator_account"/>
        <result property="createDate" column="create_date"/>
        <result property="updaterAccount" column="updater_account"/>
        <result property="updateDate" column="update_date"/>
        <result property="dataSourceSys" column="data_source_sys"/>
        <result property="materialCode" column="material_code"/>
        <result property="materialName" column="material_name"/>
        <result property="sku1Name" column="sku1_name"/>
        <result property="sku1Code" column="sku1_code"/>
        <result property="sku2Name" column="sku2_name"/>
        <result property="sku2Code" column="sku2_code"/>
        <result property="storehouseCode" column="storehouse_code"/>
        <result property="storehouseName" column="storehouse_name"/>
        <result property="locationCode" column="location_code"/>
        <result property="locationName" column="location_name"/>
        <result property="destStorehouseCode" column="dest_storehouse_code"/>
        <result property="destStorehouseName" column="dest_storehouse_name"/>
        <result property="destLocationCode" column="dest_location_code"/>
        <result property="destLocationName" column="dest_location_name"/>
    </resultMap>

    <sql id="selectInvInventoryAdjustItemVo">
        SELECT t.client_id,
               t.inventory_adjust_item_sid,
               t.inventory_adjust_sid,
               t.material_sid,
               t.sku1_sid,
               t.sku2_sid,
               t.barcode_sid,
               t.price,
               t.currency_amount,
               t.unit_base,
               t.quantity,
               t.storehouse_sid,
               t.storehouse_code,
               t.storehouse_location_sid,
               t.storehouse_location_code,
               t.reason_type,
               t.item_num,
               t.remark,
               t.creator_account,
               t.create_date,
               t.updater_account,
               t.update_date,
               t.data_source_sys,
               t.dest_material_sid,
               t.dest_sku1_sid,
               t.dest_sku2_sid,
               t.dest_barcode_sid,
               t1.material_code,
               t1.material_name,
               t2.sku_name  AS sku1_name,
               t2.sku_code  AS sku1_code,
               t3.sku_name  AS sku2_name,
               t3.sku_code  AS sku2_code,
               t4.storehouse_code,
               t4.storehouse_name,
               t5.location_code,
               t5.location_name,
               t6.name      as unit_base_name,
               t7.nick_name as creator_account_name,
               t8.barcode,
               t9.sku_name  AS dest_sku1_name,
               t10.sku_name AS dest_sku2_name
        FROM s_inv_inventory_adjust_item t
         LEFT JOIN s_bas_material t1 ON t1.material_sid = t.material_sid
         LEFT JOIN s_bas_sku t2 ON t2.sku_sid = t.sku1_sid
         LEFT JOIN s_bas_sku t3 ON t3.sku_sid = t.sku2_sid
         LEFT JOIN s_bas_storehouse t4 ON t4.storehouse_sid = t.storehouse_sid
         LEFT JOIN s_bas_storehouse_location t5 ON t5.storehouse_location_sid = t.storehouse_location_sid
         left join s_con_measure_unit t6 on t6.code = t.unit_base
         LEFT JOIN sys_user t7 ON t7.user_name = t.creator_account AND t7.client_id = t.client_id
         left join s_bas_material_barcode t8 on t8.barcode_sid = t.barcode_sid
         LEFT JOIN s_bas_sku t9 ON t9.sku_sid = t.dest_sku1_sid
         LEFT JOIN s_bas_sku t10 ON t10.sku_sid = t.dest_sku2_sid
    </sql>

    <sql id="reportInvInventoryAdjustVo">
        select t.material_sid,
               t.sku1_sid,
               t.sku2_sid,
               t.barcode_sid,
               t.unit_base,
               t.currency_amount,
               t.quantity,
               t.price,
               t.storehouse_sid,
               t.storehouse_code,
               t.storehouse_location_sid,
               t.storehouse_location_code,
               t.item_num,
               t.remark,
               t.creator_account,
               t.create_date,
               t.updater_account,
               t.update_date,
               t.data_source_sys,
               t1.material_name,
               t1.material_code,
               t2.sku_code   AS sku1_code,
               t2.sku_name   AS sku1_name,
               t3.sku_code   AS sku2_code,
               t3.sku_name   AS sku2_name,
               t4.movement_type,
               t4.document_date,
               t4.inventory_adjust_code,
               t4.inventory_adjust_sid,
               t4.account_date,
               t4.handle_status,
               t4.special_stock,
               t5.name       as movement_type_name,
               t6.name       as unit_base_name,
               t7.barcode,
               t8.storehouse_name,
               t9.location_name,
               t10.vendor_name,
               t11.customer_name,
               t12.name      as special_stock_name,
               t13.nick_name as creator_account_name
        from s_inv_inventory_adjust_item t
         LEFT JOIN s_bas_material t1 ON t1.material_sid = t.material_sid
         LEFT JOIN s_bas_sku t2 ON t2.sku_sid = t.sku1_sid
         LEFT JOIN s_bas_sku t3 ON t3.sku_sid = t.sku2_sid
         left join s_inv_inventory_adjust t4 on t4.inventory_adjust_sid = t.inventory_adjust_sid
         LEFT JOIN s_con_movement_type t5 on t5.code = t4.movement_type
         left join s_con_measure_unit t6 on t6.code = t.unit_base
         left join s_bas_material_barcode t7 on t7.barcode_sid = t.barcode_sid
         LEFT JOIN s_bas_storehouse t8 on t8.storehouse_sid = t.storehouse_sid
         LEFT JOIN s_bas_storehouse_location t9 on t9.storehouse_location_sid = t.storehouse_location_sid
         LEFT JOIN s_bas_vendor t10 ON t10.vendor_sid = t4.vendor_sid
         LEFT JOIN s_bas_customer t11 on t11.customer_sid = t4.customer_sid
         left join s_con_special_stock t12 on t12.code = t4.special_stock
         LEFT JOIN sys_user t13 ON t.creator_account = t13.user_name AND t.client_id = t13.client_id
    </sql>

    <select id="reportInvInventoryAdjust"
            parameterType="com.platform.ems.domain.dto.request.InvInventoryAdjustReportRequest"
            resultType="com.platform.ems.domain.dto.response.InvInventoryAdjustReportResponse">
        <include refid="reportInvInventoryAdjustVo"/>
        <where>
            <if test="inventoryAdjustCode != null and inventoryAdjustCode !=''">
                and (<foreach collection="inventoryAdjustCode.split(';')" item="item" separator="or">
                t4.inventory_adjust_code like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="materialCode != null and materialCode !=''">
                and (<foreach collection="materialCode.split(';')" item="item" separator="or">
                    t1.material_code like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="materialName != null and materialName !=''">
                and (<foreach collection="materialName.split(';')" item="item" separator="or">
                    t1.material_name like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="creatorAccount != null and creatorAccount !=''">
                and (<foreach collection="creatorAccount.split(';')" item="item" separator="or">
                    t.creator_account like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="vendorSidList != null and vendorSidList.length >0">
                and t4.vendor_sid in
                (<foreach collection="vendorSidList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="creatorAccountList != null and creatorAccountList.length >0 ">
                and t.creator_account in
                (<foreach collection="creatorAccountList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="customerSidList != null and customerSidList.length >0">
                and t4.customer_sid in
                (<foreach collection="customerSidList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="storehouseSid != null and storehouseSid != '' ">
                and t.storehouse_sid =#{storehouseSid}
            </if>
            <if test="storehouseSidList != null and storehouseSidList.length >0">
                and t.storehouse_sid in
                (<foreach collection="storehouseSidList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="storehouseLocationSidList != null and storehouseLocationSidList.length >0">
                and t.storehouse_location_sid in
                (<foreach collection="storehouseLocationSidList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="documentBeginTime != null and documentBeginTime!=''">
                and date_format(t4.document_date,'%y%m%d') &gt;= date_format(#{documentBeginTime},'%y%m%d')
            </if>
            <if test="documentEndTime != null and documentEndTime!=''">
                and date_format(t4.document_date,'%y%m%d') &lt;= date_format(#{documentEndTime},'%y%m%d')
            </if>
            <if test="movementTypeList != null and movementTypeList.length >0">
                and t4.movement_type in
                (<foreach collection="movementTypeList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="handleStatusList != null and handleStatusList.length >0">
                and t4.handle_status in
                (<foreach collection="handleStatusList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="specialStockList != null and specialStockList.length >0">
                and t4.special_stock in
                (<foreach collection="specialStockList" item="item" separator=",">#{item}</foreach>)
            </if>
            and t4.document_type='KCTZ'
        </where>
        order by t4.inventory_adjust_code desc
    </select>

    <sql id="reportInvCrossColorVo">
        select t.material_sid,
               t.sku1_sid,
               t.sku2_sid,
               t.barcode_sid,
               t.dest_sku1_sid,
               t.dest_sku2_sid,
               t.dest_barcode_sid,
               t.unit_base,
               t.quantity,
               t.price,
               t.storehouse_sid,
               t.storehouse_code,
               t.storehouse_location_sid,
               t.storehouse_location_code,
               t.item_num,
               t.remark,
               t.creator_account,
               t.create_date,
               t.updater_account,
               t.update_date,
               t.data_source_sys,
               t1.material_name,
               t1.material_code,
               t2.sku_code       AS sku1_code,
               t2.sku_name       AS sku1_name,
               t3.sku_code       AS sku2_code,
               t3.sku_name       AS sku2_name,
               t4.movement_type,
               t4.document_date,
               t4.inventory_adjust_sid,
               t4.account_date,
               t4.special_stock,
               t5.name           as movement_type_name,
               t6.name           as unit_base_name,
               t7.barcode,
               t8.storehouse_name,
               t9.location_name,
               t10.vendor_name,
               t11.customer_name,
               t12.sku_name      AS dest_sku1_name,
               t13.sku_name      AS dest_sku2_name,
               t14.barcode       as dest_barcode,
               t15.name          as special_stock_name,
               t16.nick_name     as creator_account_name,
               t17.material_name as dest_material_name,
               t18.inventory_document_code
        from s_inv_inventory_adjust_item t
         LEFT JOIN s_bas_material t1 ON t1.material_sid = t.material_sid
         LEFT JOIN s_bas_sku t2 ON t2.sku_sid = t.sku1_sid
         LEFT JOIN s_bas_sku t3 ON t3.sku_sid = t.sku2_sid
         left join s_inv_inventory_adjust t4 on t4.inventory_adjust_sid = t.inventory_adjust_sid
         LEFT JOIN s_con_movement_type t5 on t5.code = t4.movement_type
         left join s_con_measure_unit t6 on t6.code = t.unit_base
         left join s_bas_material_barcode t7 on t7.barcode_sid = t.barcode_sid
         LEFT JOIN s_bas_storehouse t8 on t8.storehouse_sid = t.storehouse_sid
         LEFT JOIN s_bas_storehouse_location t9 on t9.storehouse_location_sid = t.storehouse_location_sid
         LEFT JOIN s_bas_vendor t10 ON t10.vendor_sid = t4.vendor_sid
         LEFT JOIN s_bas_customer t11 on t11.customer_sid = t4.customer_sid
         LEFT JOIN s_bas_sku t12 ON t12.sku_sid = t.dest_sku1_sid
         LEFT JOIN s_bas_sku t13 ON t13.sku_sid = t.dest_sku2_sid
         left join s_bas_material_barcode t14 on t14.barcode_sid = t.dest_barcode_sid
         left join s_con_special_stock t15 on t15.code = t4.special_stock
         LEFT JOIN sys_user t16 ON t.creator_account = t16.user_name AND t.client_id = t16.client_id
         LEFT JOIN s_bas_material t17 ON t17.material_sid = t.dest_material_sid
         left join s_inv_inventory_document t18 on t18.refer_document_sid = t4.inventory_adjust_sid
    </sql>

    <select id="reportInvCrossColor" parameterType="com.platform.ems.domain.dto.request.InvCrossColorReportRequest"
            resultType="com.platform.ems.domain.dto.response.InvCrossColorReportResponse">
        <include refid="reportInvCrossColorVo"/>
        <where>
            <if test="inventoryDocumentCode != null and inventoryDocumentCode !=''">
                and (<foreach collection="inventoryDocumentCode.split(';')" item="item" separator="or">
                    t18.inventory_document_code like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="materialCode != null and materialCode !=''">
                and (<foreach collection="materialCode.split(';')" item="item" separator="or">
                    t1.material_code like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="creatorAccountList != null and creatorAccountList.length >0 ">
                and t.creator_account in
                (<foreach collection="creatorAccountList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="materialName != null and materialName !=''">
                and (<foreach collection="materialName.split(';')" item="item" separator="or">
                    t1.material_name like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="creatorAccount != null and creatorAccount !=''">
                and (<foreach collection="creatorAccount.split(';')" item="item" separator="or">
                    t.creator_account like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="vendorSidList != null and vendorSidList.length >0">
                and t4.vendor_sid in
                (<foreach collection="vendorSidList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="customerSidList != null and customerSidList.length >0">
                and t4.customer_sid in
                (<foreach collection="customerSidList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="storehouseSid != null and storehouseSid != '' ">
                and t.storehouse_sid =#{storehouseSid}
            </if>
            <if test="storehouseLocationSidList != null and storehouseLocationSidList.length >0">
                and t.storehouse_location_sid in
                (<foreach collection="storehouseLocationSidList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="documentBeginTime != null and documentBeginTime!=''">
                and date_format(t4.document_date,'%y%m%d') &gt;= date_format(#{documentBeginTime},'%y%m%d')
            </if>
            <if test="documentEndTime != null and documentEndTime!=''">
                and date_format(t4.document_date,'%y%m%d') &lt;= date_format(#{documentEndTime},'%y%m%d')
            </if>
            <if test="movementTypeList != null and movementTypeList.size >0">
                and t4.movement_type in
                (<foreach collection="movementTypeList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="handleStatusList != null and handleStatusList.length >0">
                and t4.handle_status in
                (<foreach collection="handleStatusList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="specialStockList != null and specialStockList.length >0">
                and (<foreach collection="specialStockList" item="item" separator="or">
                    t4.special_stock like concat('%', #{item}, '%')
            </foreach>)
            </if>
            and t4.document_type='CSCM'
        </where>
        order by t18.inventory_document_code desc
    </select>

    <select id="selectInvInventoryAdjustItemById" parameterType="Long"
            resultType="com.platform.ems.domain.InvInventoryAdjustItem">
        <include refid="selectInvInventoryAdjustItemVo"/>
        where t.inventory_adjust_sid = #{inventoryAdjustSid}
    </select>

    <select id="selectInvInventoryAdjustItemList" parameterType="com.platform.ems.domain.InvInventoryAdjustItem"
            resultMap="InvInventoryAdjustItemResult">
        <include refid="selectInvInventoryAdjustItemVo"/>
        <where>
            <if test="clientId != null and clientId != ''">
                and t.client_id = #{clientId}
            </if>
            <if test="inventoryAdjustSid != null ">
                and t.inventory_adjust_sid = #{inventoryAdjustSid}
            </if>
            <if test="materialSid != null ">
                and t.material_sid = #{materialSid}
            </if>
            <if test="sku1Sid != null ">
                and t.sku1_sid = #{sku1Sid}
            </if>
            <if test="sku2Sid != null ">
                and t.sku2_sid = #{sku2Sid}
            </if>
            <if test="barcodeSid != null ">
                and t.barcode_sid = #{barcodeSid}
            </if>
            <if test="unitBase != null and unitBase != ''">
                and t.unit_base = #{unitBase}
            </if>
            <if test="quantity != null ">
                and t.quantity = #{quantity}
            </if>
            <if test="storehouseSid != null ">
                and t.storehouse_sid = #{storehouseSid}
            </if>
            <if test="storehouseLocationSid != null ">
                and t.storehouse_location_sid = #{storehouseLocationSid}
            </if>
            <if test="reasonType != null and reasonType != ''">
                and (<foreach collection="reasonType.split(';')" item="item" separator="or">
                    t.reason_type like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="movementType != null ">
                and t.movement_type in
                (<foreach collection="movementType" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="itemNum != null ">
                and t.item_num = #{itemNum}
            </if>
            <if test="creatorAccount != null and creatorAccount != ''">
                and (<foreach collection="creatorAccount.split(';')" item="item" separator="or">
                    t.creator_account like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="beginTime != null and beginTime!=''">
                and date_format(t.create_date,'%y%m%d') &gt;= date_format(#{beginTime},'%y%m%d')
            </if>
            <if test="endTime != null and endTime!=''">
                and date_format(t.create_date,'%y%m%d') &lt;= date_format(#{endTime},'%y%m%d')
            </if>
            <if test="updaterAccount != null and updaterAccount != ''">
                and (<foreach collection="updaterAccount.split(';')" item="item" separator="or">
                t.updater_account like concat('%', #{item}, '%')
            </foreach>)
            </if>
            <if test="updateDate != null ">
                and t.update_date = #{updateDate}
            </if>
            <if test="dataSourceSys != null and dataSourceSys != ''">
                and t.data_source_sys = #{dataSourceSys}
            </if>
        </where>
    </select>

    <!--添加多个-->
    <insert id="inserts">
        insert into s_inv_inventory_adjust_item
        <trim prefix="(" suffix=")" suffixOverrides=",">
            client_id,
            inventory_adjust_item_sid,
            inventory_adjust_sid,
            material_sid,
            sku1_sid,
            sku2_sid,
            barcode_sid,
            unit_base,
            quantity,
            storehouse_sid,
            storehouse_code,
            storehouse_location_sid,
            storehouse_location_code,
            reason_type,
            stock_type,
            special_stock,
            movement_type,
            item_num,
            remark,
            creator_account,
            create_date,
            updater_account,
            update_date,
            data_source_sys,
            price,
            currency_amount,
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <foreach collection="list" item="o" separator=",">
                #{o.clientId},
                #{o.inventoryAdjustItemSid},
                #{o.inventoryAdjustSid},
                #{o.materialSid},
                #{o.sku1Sid},
                #{o.sku2Sid},
                #{o.barcodeSid},
                #{o.unitBase},
                #{o.quantity},
                #{o.storehouseSid},
                #{o.storehouseCode},
                #{o.storehouseLocationSid},
                #{o.storehouseLocationCode},
                #{o.reasonType},
                #{o.stockType},
                #{o.specialStock},
                #{o.movementType},
                #{o.itemNum},
                #{o.remark},
                #{o.creatorAccount},
                #{o.createDate},
                #{o.updaterAccount},
                #{o.updateDate},
                #{o.dataSourceSys},
                #{o.price},
                #{o.currencyAmount},
            </foreach>
        </trim>
    </insert>

    <!--更新-->
    <update id="updateAllById">
        update s_inv_inventory_adjust_item
        <trim prefix="SET" suffixOverrides=",">
            client_id = #{clientId},
            inventory_adjust_sid = #{inventoryAdjustSid},
            material_sid = #{materialSid},
            sku1_sid = #{sku1Sid},
            sku2_sid = #{sku2Sid},
            barcode_sid = #{barcodeSid},
            unit_base = #{unitBase},
            quantity = #{quantity},
            storehouse_sid = #{storehouseSid},
            storehouse_code = #{storehouseCode},
            storehouse_location_sid = #{storehouseLocationSid},
            storehouse_location_code = #{storehouseLocationCode},
            reason_type = #{reasonType},
            stock_type = #{stockType},
            special_stock = #{specialStock},
            movement_type = #{movementType},
            item_num = #{itemNum},
            remark = #{remark},
            creator_account = #{creatorAccount},
            create_date = #{createDate},
            updater_account = #{updaterAccount},
            update_date = #{updateDate},
            data_source_sys = #{dataSourceSys},
            price=#{price},
            currency_amount=#{currencyAmount},
        </trim>
        where inventory_adjust_item_sid = #{inventoryAdjustItemSid}
    </update>

    <!--更新多个-->
    <update id="updatesAllById">
        <foreach collection="list" item="o" separator=";">
            update s_inv_inventory_adjust_item
            <trim prefix="SET" suffixOverrides=",">
                client_id = #{clientId},
                inventory_adjust_sid = #{inventoryAdjustSid},
                material_sid = #{materialSid},
                sku1_sid = #{sku1Sid},
                sku2_sid = #{sku2Sid},
                barcode_sid = #{barcodeSid},
                unit_base = #{unitBase},
                quantity = #{quantity},
                storehouse_sid = #{storehouseSid},
                storehouse_code = #{storehouseCode},
                storehouse_location_sid = #{storehouseLocationSid},
                storehouse_location_code = #{storehouseLocationCode},
                reason_type = #{reasonType},
                stock_type = #{stockType},
                special_stock = #{specialStock},
                movement_type = #{movementType},
                item_num = #{itemNum},
                remark = #{remark},
                creator_account = #{creatorAccount},
                create_date = #{createDate},
                updater_account = #{updaterAccount},
                update_date = #{updateDate},
                data_source_sys = #{dataSourceSys},
            </trim>
            where inventory_adjust_item_sid = #{inventoryAdjustItemSid}
        </foreach>
    </update>

    <delete id="deleteInvInventoryAdjustItemByIds" parameterType="long">
        delete from s_inv_inventory_adjust_item where inventory_adjust_sid in
        <foreach item="inventoryAdjustSid" collection="array" open="(" separator="," close=")">
            #{inventoryAdjustSid}
        </foreach>
    </delete>

</mapper>