<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.ems.mapper.TecLinePositionGroupMapper">

    <resultMap type="com.platform.ems.domain.TecLinePositionGroup" id="TecLinePositionGroupResult">
        <result property="clientId" column="client_id"/>
        <result property="groupSid" column="group_sid"/>
        <result property="groupCode" column="group_code"/>
        <result property="groupName" column="group_name"/>
        <result property="upDownSuit" column="up_down_suit"/>
        <result property="remark" column="remark"/>
        <result property="status" column="status"/>
        <result property="handleStatus" column="handle_status"/>
        <result property="creatorAccount" column="creator_account"/>
        <result property="createDate" column="create_date"/>
        <result property="updaterAccount" column="updater_account"/>
        <result property="updateDate" column="update_date"/>
        <result property="confirmerAccount" column="confirmer_account"/>
        <result property="confirmDate" column="confirm_date"/>
        <result property="dataSourceSys" column="data_source_sys"/>
        <result property="creatorAccountName" column="creator_account_name"/>
        <result property="updaterAccountName" column="updater_account_name"/>
        <result property="confirmerAccountName" column="confirmer_account_name"/>
        <result property="disableRemark" column="disable_remark"/>
    </resultMap>

    <sql id="selectTecLinePositionGroupVo">
        select
        t.client_id,
        t.group_sid,
        t.group_code,
        t.group_name,
        t.up_down_suit,
        t.remark,
        t.disable_remark,
        t.status,
        t.handle_status,
        t.creator_account,
        t.create_date,
        t.updater_account,
        t.update_date,
        t.confirmer_account,
        t.confirm_date,
        t.data_source_sys,
        t1.nick_name as creator_account_name,
        t2.nick_name as updater_account_name,
        t3.nick_name as confirmer_account_name
  from s_tec_line_position_group t
        LEFT JOIN sys_user t1 ON t1.user_name = t.creator_account
	    LEFT JOIN sys_user t2 ON t2.user_name = t.updater_account
	    LEFT JOIN sys_user t3 ON t3.user_name = t.confirmer_account
    </sql>

    <select id="selectTecLinePositionGroupById" parameterType="Long" resultMap="TecLinePositionGroupResult">
        <include refid="selectTecLinePositionGroupVo"/>
        where t.group_sid = #{groupSid}
    </select>


    <select id="selectTecLinePositionGroupList" parameterType="com.platform.ems.domain.TecLinePositionGroup"
            resultMap="TecLinePositionGroupResult">
        <include refid="selectTecLinePositionGroupVo"/>
        <where>
            <if test="clientId != null  and clientId != ''">
                and (<foreach collection="clientId.split(';')" item="item" separator="or">t.client_id like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="groupSid != null ">
                and t.group_sid = #{groupSid}
            </if>
            <if test="groupSidList != null and groupSidList.length >0 ">
                and t.group_sid in
                (<foreach collection="groupSidList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="groupCode != null">
                and t.group_code like concat('%',#{groupCode}, '%')
            </if>
            <if test="groupName != null  and groupName != ''">
                and (<foreach collection="groupName.split(';')" item="item" separator="or">t.group_name like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="upDownSuit != null  and upDownSuit != ''">
                and (<foreach collection="upDownSuit.split(';')" item="item" separator="or">t.up_down_suit like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="upDownSuitList != null and upDownSuitList.length >0 ">
                and t.up_down_suit in
                (<foreach collection="upDownSuitList" item="upDownSuit" separator=",">#{upDownSuit}</foreach>)
            </if>
            <if test="status != null  and status != ''">
                and (<foreach collection="status.split(';')" item="item" separator="or">t.status like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="handleStatus != null  and handleStatus != ''">
                and (<foreach collection="handleStatus.split(';')" item="item" separator="or">t.handle_status like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="handleStatusList != null and handleStatusList.length >0 ">
                and t.handle_status in
                (<foreach collection="handleStatusList" item="handleStatus" separator=",">#{handleStatus}</foreach>)
            </if>
            <if test="creatorAccount != null  and creatorAccount != ''">
                and (<foreach collection="creatorAccount.split(';')" item="item" separator="or">t1.nick_name like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="beginTime != null and beginTime!=''">
                and date_format(t.create_date,'%y%m%d') &gt;= date_format(#{beginTime},'%y%m%d')
            </if>
            <if test="endTime != null and endTime!=''">
                and date_format(t.create_date,'%y%m%d') &lt;= date_format(#{endTime},'%y%m%d')
            </if>
            <if test="updaterAccount != null  and updaterAccount != ''">
                and (<foreach collection="updaterAccount.split(';')" item="item" separator="or">t.updater_account like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="updateDate != null ">
                and t.update_date = #{updateDate}
            </if>
            <if test="confirmerAccount != null  and confirmerAccount != ''">
                and (<foreach collection="confirmerAccount.split(';')" item="item" separator="or">t.confirmer_account
                like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="confirmDate != null ">
                and t.confirm_date = #{confirmDate}
            </if>
            <if test="dataSourceSys != null  and dataSourceSys != ''">
                and (<foreach collection="dataSourceSys.split(';')" item="item" separator="or">t.data_source_sys like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="params.dataScope != null and params.dataScope!=''">
                ${params.dataScope}
            </if>
        </where>
        order by t.group_code desc
    </select>


    <!--添加多个-->
    <insert id="inserts">
        insert into s_tec_line_position_group
        <trim prefix="(" suffix=")" suffixOverrides=",">
            client_id,
            group_code,
            group_name,
            up_down_suit,
            remark,
            disable_remark,
            status,
            handle_status,
            creator_account,
            create_date,
            updater_account,
            update_date,
            confirmer_account,
            confirm_date,
            data_source_sys,
        </trim>
        values
        <foreach collection="list" item="item" separator=",">
            <trim prefix=" (" suffix=")" suffixOverrides=",">
                #{item.clientId},
                #{item.groupCode},
                #{item.groupName},
                #{item.upDownSuit},
                #{item.remark},
                #{item.disableRemark},
                #{item.status},
                #{item.handleStatus},
                #{item.creatorAccount},
                #{item.createDate},
                #{item.updaterAccount},
                #{item.updateDate},
                #{item.confirmerAccount},
                #{item.confirmDate},
                #{item.dataSourceSys},
            </trim>
        </foreach>
    </insert>

    <!--更新-->
    <update id="updateAllById">
        update s_tec_line_position_group
        <trim prefix="SET" suffixOverrides=",">
            group_sid = #{groupSid},
            group_code = #{groupCode},
            group_name = #{groupName},
            up_down_suit = #{upDownSuit},
            remark = #{remark},
            disable_remark = #{disableRemark},
            status = #{status},
            handle_status = #{handleStatus},
            updater_account = #{updaterAccount},
            update_date = #{updateDate},
            confirmer_account = #{confirmerAccount},
            confirm_date = #{confirmDate},
            data_source_sys = #{dataSourceSys},
        </trim>
        where group_sid = #{groupSid}
    </update>

    <!--更新多个-->
    <update id="updatesAllById">
        <foreach collection="list" item="o" separator=";">
            update s_tec_line_position_group
            <trim prefix="SET" suffixOverrides=",">
                client_id = #{clientId},
                group_sid = #{groupSid},
                group_code = #{groupCode},
                group_name = #{groupName},
                up_down_suit = #{upDownSuit},
                remark = #{remark},
                disable_remark = #{disableRemark},
                status = #{status},
                handle_status = #{handleStatus},
                creator_account = #{creatorAccount},
                create_date = #{createDate},
                updater_account = #{updaterAccount},
                update_date = #{updateDate},
                confirmer_account = #{confirmerAccount},
                confirm_date = #{confirmDate},
                data_source_sys = #{dataSourceSys},
            </trim>
            where group_sid = #{groupSid}
        </foreach>
    </update>

</mapper>
