<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.ems.mapper.BasMaterialAttachmentMapper">

    <resultMap type="com.platform.ems.domain.BasMaterialAttachment" id="BasMaterialAttachmentResult">
        <result property="clientId" column="client_id"/>
        <result property="materialAttachmentSid" column="material_attachment_sid"/>
        <result property="materialSid" column="material_sid"/>
        <result property="fileType" column="file_type"/>
        <result property="fileName" column="file_name"/>
        <result property="filePath" column="file_path"/>
        <result property="remark" column="remark"/>
        <result property="creatorAccount" column="creator_account"/>
        <result property="createDate" column="create_date"/>
        <result property="updaterAccount" column="updater_account"/>
        <result property="updateDate" column="update_date"/>
        <result property="dataSourceSys" column="data_source_sys"/>
        <result property="fileTypeName" column="file_type_name"/>
        <result property="creatorAccountName" column="creator_account_name"/>
    </resultMap>

    <select id="selectBasMaterialAttachmentById" parameterType="Long" resultMap="BasMaterialAttachmentResult">
        <include refid="selectBasMaterialAttachmentVo"/>
        where t.material_attachment_sid = #{materialAttachmentSid}
    </select>

    <select id="selectBasMaterialAttachmentList" parameterType="com.platform.ems.domain.BasMaterialAttachment"
            resultMap="BasMaterialAttachmentResult">
        <include refid="selectBasMaterialAttachmentVo"/>
        <where>
            <if test="clientId != null and clientId != ''">
                and t.client_id = #{clientId}
            </if>
            <if test="materialSid != null ">and t.material_sid = #{materialSid}</if>
            <if test="materialSidList != null and materialSidList.length >0 ">
                and t.material_sid in
                (<foreach collection="materialSidList" item="materialSid" separator=",">#{materialSid}</foreach>)
            </if>
            <if test="fileType != null and fileType != ''">
                and t.file_type = #{fileType}
            </if>
            <if test="fileName != null and fileName != ''">and t.file_name like concat('%', #{fileName}, '%')</if>
            <if test="filePath != null and filePath != ''">
                and t.file_path = #{filePath}
            </if>
            <if test="creatorAccount != null and creatorAccount != ''">
                and t.creator_accoun = #{creatorAccount}
            </if>
            <if test="createDate != null ">and t.create_date = #{createDate}</if>
            <if test="updateDate != null ">and t.update_date = #{updateDate}</if>
            <if test="dataSourceSys != null and dataSourceSys != ''">
                and t.data_source_sys = #{dataSourceSys}
            </if>
        </where>
    </select>

    <!--添加多个-->
    <insert id="inserts">
        insert into s_bas_material_attachment
        <trim prefix="(" suffix=")" suffixOverrides=",">
            client_id,
            material_attachment_sid,
            material_sid,
            file_type,
            file_name,
            file_path,
            remark,
            creator_account,
            create_date,
            updater_account,
            update_date,
            data_source_sys,
        </trim>
        values
        <foreach collection="list" item="item" separator=",">
            <trim prefix=" (" suffix=")" suffixOverrides=",">
                #{item.clientId},
                #{item.materialAttachmentSid},
                #{item.materialSid},
                #{item.fileType},
                #{item.fileName},
                #{item.filePath},
                #{item.remark},
                #{item.creatorAccount},
                #{item.createDate},
                #{item.updaterAccount},
                #{item.updateDate},
                #{item.dataSourceSys},
            </trim>
        </foreach>
    </insert>

    <!--更新-->
    <update id="updateAllById">
        update s_bas_material_attachment
        <trim prefix="SET" suffixOverrides=",">
            client_id = #{clientId},
            material_sid = #{materialSid},
            file_type = #{fileType},
            file_name = #{fileName},
            file_path = #{filePath},
            remark = #{remark},
            creator_account = #{creatorAccount},
            create_date = #{createDate},
            updater_account = #{updaterAccount},
            update_date = #{updateDate},
            data_source_sys = #{dataSourceSys},
        </trim>
        where material_attachment_sid = #{materialAttachmentSid}
    </update>

    <!--更新多个-->
    <update id="updatesAllById">
        <foreach collection="list" item="o" separator=";">
            update s_bas_material_attachment
            <trim prefix="SET" suffixOverrides=",">
                client_id = #{clientId},
                material_sid = #{materialSid},
                file_type = #{fileType},
                file_name = #{fileName},
                file_path = #{filePath},
                remark = #{remark},
                creator_account = #{creatorAccount},
                create_date = #{createDate},
                updater_account = #{updaterAccount},
                update_date = #{updateDate},
                data_source_sys = #{dataSourceSys},
            </trim>
            where material_attachment_sid = #{materialAttachmentSid}
        </foreach>
    </update>

    <sql id="selectBasMaterialAttachmentVo">
        SELECT
	t.client_id,
	t.material_attachment_sid,
	t.material_sid,
	t.file_type,
	t.file_name,
	t.file_path,
	t.remark,
	t.creator_account,
	t.create_date,
	t.updater_account,
	t.update_date,
	t.data_source_sys,
    t1.name AS file_type_name,
    t2.nick_name AS creator_account_name,
    t3.material_code
FROM
	s_bas_material_attachment t
	LEFT JOIN s_con_file_type t1 on t1.code = t.file_type and t1.dataobject_category_code = 'Material'
    LEFT JOIN sys_user t2 on t.creator_account = t2.user_name
    LEFT JOIN s_bas_material t3 on t.material_sid = t3.material_sid
    </sql>

    <delete id="deleteBasMaterialAttachmentByIds" parameterType="long">
        delete from s_bas_material_attachment where material_sid in
        <foreach item="materialSid" collection="array" open="(" separator="," close=")">
            #{materialSid}
        </foreach>
    </delete>

</mapper>
