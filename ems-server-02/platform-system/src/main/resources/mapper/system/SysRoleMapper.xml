<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.system.mapper.SysRoleMapper">

    <resultMap type="com.platform.common.core.domain.entity.SysRole" id="SysRoleResult">
        <id property="roleId" column="role_id"/>
        <result property="roleName" column="role_name"/>
        <result property="roleKey" column="role_key"/>
        <result property="roleSort" column="role_sort"/>
        <result property="dataScope" column="data_scope"/>
        <result property="menuCheckStrictly" column="menu_check_strictly"/>
        <result property="deptCheckStrictly" column="dept_check_strictly"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
        <result property="clientId" column="client_id"/>
        <result property="creatorAccountName" column="creator_account_name"/>
    </resultMap>

    <sql id="selectRoleVo">
        select distinct r.role_id,
                        r.role_type,
                        r.role_name,
                        r.role_key,
                        r.role_sort,
                        r.data_scope,
                        r.menu_check_strictly,
                        r.dept_check_strictly,
                        r.client_id,
                        r.status,
                        r.del_flag,
                        r.create_by,
                        r.create_time,
                        r.remark,
                        c.nick_name as creator_account_name
        from sys_role r
                 left join sys_user_role ur on ur.role_id = r.role_id
                 left join sys_user u on u.user_id = ur.user_id
                 left join sys_dept d on u.dept_id = d.dept_id
                 left join sys_user c on r.create_by = c.user_name
    </sql>

    <select id="selectRoleList" parameterType="com.platform.common.core.domain.entity.SysRole"
            resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where r.del_flag = '0'
        <if test="clientId != null and clientId != ''">
            AND r.client_id = #{clientId}
        </if>
        <if test="roleId != null">
            AND r.role_id = #{roleId}
        </if>
        <if test="roleName != null and roleName != ''">
            AND r.role_name like concat('%', #{roleName}, '%')
        </if>
        <if test="roleType != null and roleType != ''">
            AND r.role_type = #{roleType}
        </if>
        <if test="status != null and status != ''">
            AND r.status = #{status}
        </if>
        <if test="createBy != null and createBy != ''">
            AND r.create_by = #{createBy}
        </if>
        <if test="roleKey != null and roleKey != ''">
            AND r.role_key like concat('%', #{roleKey}, '%')
        </if>
        <if test="beginTime != null and beginTime != ''"><!-- 开始时间检索 -->
            and date_format(r.create_time,'%y%m%d') &gt;= date_format(#{beginTime},'%y%m%d')
        </if>
        <if test="endTime != null and endTime != ''"><!-- 结束时间检索 -->
            and date_format(r.create_time,'%y%m%d') &lt;= date_format(#{endTime},'%y%m%d')
        </if>
        <!-- 数据范围过滤 -->
        ${params.dataScope}
        order by r.role_id asc
    </select>
    <sql id="selectRoleReportVo">
        select u.user_name,
               u.user_type,
               u.account_type,
               u.client_id,
               u.user_id,
               u.status,
               u.nick_name,
               r.role_id,
               r.role_type,
               r.role_name,
               r.role_key,
               r.status      as role_status,
               t2.staff_name,
               t3.short_name as vendor_name,
               t4.short_name as customer_name
        from sys_role r
                 left join sys_user_role ur on ur.role_id = r.role_id
                 left join sys_user u on u.user_id = ur.user_id
                 left join s_bas_staff t2 on t2.staff_sid = u.staff_sid
                 left join s_bas_vendor t3 on t3.vendor_sid = u.vendor_sid
                 left join s_bas_customer t4 on t4.customer_sid = u.customer_sid
    </sql>
    <select id="selectRoleReport" parameterType="com.platform.system.domain.dto.request.SysRoleRequest"
            resultType="com.platform.system.domain.dto.response.SysRoleResponse">
        <include refid="selectRoleReportVo"/>
        <where>
            u.user_name is not null
            <if test="userNameList != null and userNameList.length >0">
                AND u.user_name in
                (<foreach collection="userNameList" item="item" separator=",">#{item}</foreach>)
            </if>

            <if test="userName!= null and userName !=''">
                AND u.user_name like concat('%', #{userName}, '%')
            </if>
            <if test="roleName != null and roleName !=''">
                AND r.role_name like concat('%', #{roleName}, '%')
            </if>
            <if test="roleNameList != null and roleNameList.length >0">
                AND r.role_name in
                (<foreach collection="roleNameList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="roleTypeList != null and roleTypeList.length >0">
                AND r.role_type in
                (<foreach collection="roleTypeList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="userTypeList != null and userTypeList.length >0">
                AND u.account_type in
                (<foreach collection="userTypeList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="staffSidList != null and staffSidList.length >0">
                AND u.staff_sid in
                (<foreach collection="staffSidList" item="item" separator=",">#{item}</foreach>)

            </if>
            <if test="vendorSidList != null and vendorSidList.length >0">
                AND u.vendor_sid in
                (<foreach collection="vendorSidList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="customerSidList != null and customerSidList.length >0">
                AND u.customer_sid in
                (<foreach collection="customerSidList" item="item" separator=",">#{item}</foreach>)
            </if>
            <if test="nickName != null and nickName != ''">
                AND u.nick_name like concat('%', #{nickName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND u.status like concat('%', #{status}, '%')
            </if>
            <if test="roleStatus != null and roleStatus != ''">
                AND r.status like concat('%', #{roleStatus}, '%')
            </if>
            <if test="clientId != null and clientId != ''">
                AND u.client_id like concat('%', #{clientId}, '%')
            </if>
            <if test="roleKey != null and roleKey != ''">
                AND r.role_key like concat('%', #{roleKey}, '%')
            </if>
        </where>
    </select>

    <select id="selectRolePermissionByUserId" parameterType="Long" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        WHERE r.del_flag = '0' and ur.user_id = #{userId}
    </select>

    <select id="selectRoleAll" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
    </select>

    <select id="selectRoleListByUserId" parameterType="Long" resultType="Integer">
        select r.role_id
        from sys_role r
                 left join sys_user_role ur on ur.role_id = r.role_id
                 left join sys_user u on u.user_id = ur.user_id
        where u.user_id = #{userId}
          and r.status = '0'
    </select>

    <select id="selectRoleById" parameterType="Long" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where r.role_id = #{roleId}
    </select>

    <select id="selectRolesByUserName" parameterType="String" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        WHERE r.del_flag = '0' and u.user_name = #{userName}
    </select>

    <select id="checkRoleNameUnique" parameterType="com.platform.common.core.domain.entity.SysRole"
            resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where r.role_name=#{roleName} and r.client_id=#{clientId} limit 1
    </select>

    <select id="checkRoleKeyUnique" parameterType="com.platform.common.core.domain.entity.SysRole"
            resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where r.role_key=#{roleKey} and r.client_id=#{clientId} limit 1
    </select>

    <select id="checkRoleTypeUnique" parameterType="String" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where r.role_type=#{roleType} and r.client_id=#{clientId}
    </select>

    <insert id="insertRole" parameterType="com.platform.common.core.domain.entity.SysRole" useGeneratedKeys="true"
            keyProperty="roleId">
        insert into sys_role(
        <if test="roleId != null and roleId != 0">role_id,</if>
        <if test="roleName != null and roleName != ''">role_name,</if>
        <if test="roleKey != null and roleKey != ''">role_key,</if>
        <if test="roleSort != null and roleSort != ''">role_sort,</if>
        <if test="dataScope != null and dataScope != ''">data_scope,</if>
        <if test="menuCheckStrictly != null">menu_check_strictly,</if>
        <if test="deptCheckStrictly != null">dept_check_strictly,</if>
        <if test="status != null and status != ''">status,</if>
        <if test="remark != null and remark != ''">remark,</if>
        <if test="createBy != null and createBy != ''">create_by,</if>
        <if test="roleType != null and roleType != ''">role_type,</if>
        <if test="clientId != null and clientId != ''">client_id,</if>
        create_time
        )values(
        <if test="roleId != null and roleId != 0">#{roleId},</if>
        <if test="roleName != null and roleName != ''">#{roleName},</if>
        <if test="roleKey != null and roleKey != ''">#{roleKey},</if>
        <if test="roleSort != null and roleSort != ''">#{roleSort},</if>
        <if test="dataScope != null and dataScope != ''">#{dataScope},</if>
        <if test="menuCheckStrictly != null">#{menuCheckStrictly},</if>
        <if test="deptCheckStrictly != null">#{deptCheckStrictly},</if>
        <if test="status != null and status != ''">#{status},</if>
        <if test="remark != null and remark != ''">#{remark},</if>
        <if test="createBy != null and createBy != ''">#{createBy},</if>
        <if test="roleType != null and roleType != ''">#{roleType},</if>
        <if test="clientId != null and clientId != ''">#{clientId},</if>
        sysdate()
        )
    </insert>

    <update id="updateRole" parameterType="com.platform.common.core.domain.entity.SysRole">
        update sys_role
        <set>
            <if test="roleName != null and roleName != ''">role_name = #{roleName},</if>
            <if test="roleKey != null and roleKey != ''">role_key = #{roleKey},</if>
            <if test="roleSort != null and roleSort != ''">role_sort = #{roleSort},</if>
            <if test="dataScope != null and dataScope != ''">data_scope = #{dataScope},</if>
            <if test="menuCheckStrictly != null">menu_check_strictly = #{menuCheckStrictly},</if>
            <if test="deptCheckStrictly != null">dept_check_strictly = #{deptCheckStrictly},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            role_type = #{roleType},
            update_time = sysdate()
        </set>
        where role_id = #{roleId}
    </update>

    <delete id="deleteRoleById" parameterType="Long">
        delete
        from sys_role
        where role_id = #{roleId}
    </delete>

    <delete id="removeRole" parameterType="com.platform.system.domain.dto.response.SysRoleResponse">
        delete
        from sys_user_role
        where role_id = #{roleId}
          and client_id = #{clientId}
          and user_id = #{userId}
    </delete>
    <insert id="addRole" parameterType="com.platform.system.domain.dto.response.SysRoleResponse">
        insert into sys_user_role(
        <if test="roleId != null and roleId != 0">role_id,</if>
        <if test="clientId != null and clientId != ''">client_id,</if>
        <if test="userId != null and userId != ''">user_id</if>
        )values(
        <if test="roleId != null and roleId != 0">#{roleId},</if>
        <if test="clientId != null and clientId != ''">#{clientId},</if>
        <if test="userId != null and userId != ''">#{userId}</if>
        )
    </insert>
    <delete id="deleteRoleByIds" parameterType="Long">
        update sys_role set del_flag = '2' where role_id in
        <foreach collection="array" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </delete>

</mapper>
