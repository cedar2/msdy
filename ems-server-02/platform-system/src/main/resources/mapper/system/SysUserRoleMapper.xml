<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.system.mapper.SysUserRoleMapper">

	<resultMap type="com.platform.common.core.domain.entity.SysUserRole" id="SysUserRoleResult">
		<result property="userId"     column="user_id"      />
		<result property="roleId"     column="role_id"      />
	</resultMap>

	<delete id="deleteUserRoleByUserId" parameterType="Long">
		delete from sys_user_role where user_id=#{userId}
	</delete>

	<select id="countUserRoleByRoleId" resultType="Integer">
	    select count(1) from sys_user_role where role_id=#{roleId}
	</select>

	<delete id="deleteUserRole" parameterType="Long">
 		delete from sys_user_role where user_id in
 		<foreach collection="array" item="userId" open="(" separator="," close=")">
 			#{userId}
        </foreach>
 	</delete>

	<insert id="batchUserRole">
		insert into sys_user_role(user_id, role_id) values
		<foreach item="item" index="index" collection="list" separator=",">
			(#{item.userId},#{item.roleId})
		</foreach>
	</insert>

	<delete id="deleteUserRoleInfo" parameterType="com.platform.common.core.domain.entity.SysUserRole">
		delete from sys_user_role where user_id=#{userId} and role_id=#{roleId}
	</delete>

	<delete id="deleteUserRoleInfos">
	    delete from sys_user_role where role_id=#{roleId} and user_id in
 	    <foreach collection="userIds" item="userId" open="(" separator="," close=")">
 	        #{userId}
            </foreach>
	</delete>


	<!--添加多个-->
	<insert id="inserts">
		insert into sys_user_role
		<trim prefix="(" suffix=")" suffixOverrides=",">
			client_id,
			user_id,
			role_id,
			create_by,
			create_time,
			data_source_sys,
		</trim>
		values
		<foreach collection="list" item="item" separator=",">
			<trim prefix=" (" suffix=")" suffixOverrides=",">
				#{item.clientId},
				#{item.userId},
				#{item.roleId},
				#{item.createBy},
				#{item.createTime},
				#{item.dataSourceSys},
			</trim>
		</foreach>
	</insert>

	<select id="selectSysUserRoleList" parameterType="com.platform.common.core.domain.entity.SysUserRole"
			resultType="com.platform.common.core.domain.entity.SysUserRole">
		select t.client_id,
		t.user_id,
		t.role_id,
		t.create_by,
		t.create_time,
		t.data_source_sys,
		t1.user_name,
		t1.nick_name,
		t2.role_key,
		t2.role_name,
		t2.status,
		t2.remark,
		t3.nick_name as creator_account_name
		from sys_user_role t
		LEFT JOIN sys_user t1 ON t.user_id = t1.user_id
		LEFT JOIN sys_role t2 ON t.role_id = t2.role_id
		LEFT JOIN sys_user t3 ON t.create_by = t3.user_name AND t.client_id = t3.client_id
		<where>
			<if test="clientId != null and clientId != ''">
				and t.client_id = #{clientId}
			</if>
			<if test="userId != null ">
				and t.user_id = #{userId}
			</if>
			<if test="userIdList != null and userIdList.length >0 ">
				and t.user_id in
				(<foreach collection="userIdList" item="item" separator=",">#{item}</foreach>)
			</if>
			<if test="userName != null and userName != ''">
				and t1.user_name like concat('%', #{userName}, '%')
			</if>
			<if test="roleId != null ">
				and t.role_id = #{roleId}
			</if>
			<if test="roleIdList != null and roleIdList.length >0 ">
				and t.role_id in
				(<foreach collection="roleIdList" item="item" separator=",">#{item}</foreach>)
			</if>
			<if test="dataSourceSys != null and dataSourceSys != ''">
				and t.data_source_sys = #{dataSourceSys}
			</if>
		</where>
	</select>

</mapper>
