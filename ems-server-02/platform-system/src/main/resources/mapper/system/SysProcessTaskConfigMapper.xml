<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.system.mapper.SysProcessTaskConfigMapper">

    <resultMap type="com.platform.system.domain.SysProcessTaskConfig" id="SysProcessTaskConfigResult">
        <result property="clientId" column="client_id"/>
        <result property="id" column="id"/>
        <result property="processKey" column="process_key"/>
        <result property="taskId" column="task_id"/>
        <result property="taskName" column="task_name"/>
        <result property="propertiesId" column="properties_id"/>
        <result property="remark" column="remark"/>
        <result property="createDate" column="create_date"/>
        <result property="modifyDate" column="modify_date"/>
        <result property="creatorAccount" column="creator_account"/>
        <result property="updaterAccount" column="updater_account"/>
        <result property="createById" column="create_by_id"/>
        <result property="modifyById" column="modify_by_id"/>
        <result property="status" column="status"/>
        <collection property="propertiesConfigList" ofType="com.platform.system.domain.SysProcessTaskPropertiesConfig">
            <result property="name" column="name"/>
            <result property="value" column="value"/>
        </collection>
    </resultMap>

    <sql id="selectSysProcessTaskConfigVo">
        select
        t.client_id,
        t.id,
        t.process_key,
        t.task_id,
        t.task_name,
        t.properties_id,
        t.remark,
        t.create_date,
        t.modify_date,
        t.creator_account,
        t.updater_account,
        t.create_by_id,
        t.modify_by_id,
        t.status
        from s_sys_process_task_config t
    </sql>

    <sql id="selectChildVo">
        SELECT
        t.client_id,
        t.id,
        t.process_key,
        t.task_id,
        t.task_name,
        t.properties_id,
        t.remark,
        t.create_date,
        t.modify_date,
        t.creator_account,
        t.updater_account,
        t.create_by_id,
        t.modify_by_id,
        t.STATUS,
        t2.name,
        t2.value
        FROM
        s_sys_process_task_config t
        LEFT JOIN s_sys_process_task_properties_config t2 ON t.properties_id = t2.id
    </sql>

    <select id="selectSysProcessTaskConfigById" parameterType="Long" resultMap="SysProcessTaskConfigResult">
        <include refid="selectSysProcessTaskConfigVo"/>
        where t.id = #{id}
    </select>


    <select id="selectSysProcessTaskConfigList" parameterType="com.platform.system.domain.SysProcessTaskConfig"
            resultMap="SysProcessTaskConfigResult">
        <include refid="selectChildVo"/>
        <where>
            <if test="clientId != null  and clientId != ''">
                and (<foreach collection="clientId.split(';')" item="item" separator="or">t.client_id like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="processKey != null  and processKey != ''">
                and (<foreach collection="processKey.split(';')" item="item" separator="or">t.process_key like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="taskId != null  and taskId != ''">
                and (<foreach collection="taskId.split(';')" item="item" separator="or">t.task_id like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="taskName != null  and taskName != ''">and
                t.task_name like concat('%', #{taskName}, '%')
            </if>
            <if test="propertiesId != null ">
                and t.properties_id =#{propertiesId}
            </if>
            <if test="beginTime != null and beginTime!=''">
                and date_format(t.create_date,'%y%m%d') &gt;= date_format(#{beginTime},'%y%m%d')
            </if>
            <if test="endTime != null and endTime!=''">
                and date_format(t.create_date,'%y%m%d') &lt;= date_format(#{endTime},'%y%m%d')
            </if>
            <if test="modifyDate != null ">
                and t.modify_date = #{modifyDate}
            </if>
            <if test="creatorAccount != null  and creatorAccount != ''">
                and (<foreach collection="creatorAccount.split(';')" item="item" separator="or">t.creator_account like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="updaterAccount != null  and updaterAccount != ''">
                and (<foreach collection="updaterAccount.split(';')" item="item" separator="or">t.updater_account like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="createById != null  and createById != ''">
                and (<foreach collection="createById.split(';')" item="item" separator="or">t.create_by_id like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="modifyById != null  and modifyById != ''">
                and (<foreach collection="modifyById.split(';')" item="item" separator="or">t.modify_by_id like
                concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="status != null  and status != ''">
                and (<foreach collection="status.split(';')" item="item" separator="or">t.status like concat('%',
                #{item}, '%')
            </foreach>)
            </if>
            <if test="params.dataScope != null and params.dataScope!=''">
                ${params.dataScope}
            </if>
        </where>
    </select>


    <!--添加多个-->
    <insert id="inserts">
        insert into s_sys_process_task_config
        <trim prefix="(" suffix=")" suffixOverrides=",">
            client_id,
            process_key,
            task_id,
            task_name,
            properties_id,
            remark,
            create_date,
            modify_date,
            creator_account,
            updater_account,
            create_by_id,
            modify_by_id,
            status,
        </trim>
        values
        <foreach collection="list" item="item" separator=",">
            <trim prefix=" (" suffix=")" suffixOverrides=",">
                #{item.clientId},
                #{item.processKey},
                #{item.taskId},
                #{item.taskName},
                #{item.propertiesId},
                #{item.remark},
                #{item.createDate},
                #{item.modifyDate},
                #{item.creatorAccount},
                #{item.updaterAccount},
                #{item.createById},
                #{item.modifyById},
                #{item.status},
            </trim>
        </foreach>
    </insert>

    <!--更新-->
    <update id="updateAllById">
        update s_sys_process_task_config
        <trim prefix="SET" suffixOverrides=",">
            client_id = #{clientId},
            id = #{id},
            process_key = #{processKey},
            task_id = #{taskId},
            task_name = #{taskName},
            properties_id = #{propertiesId},
            remark = #{remark},
            create_date = #{createDate},
            modify_date = #{modifyDate},
            creator_account = #{creatorAccount},
            updater_account = #{updaterAccount},
            create_by_id = #{createById},
            modify_by_id = #{modifyById},
            status = #{status},
        </trim>
        where id = #{id}
    </update>

    <!--更新多个-->
    <update id="updatesAllById">
        <foreach collection="list" item="o" separator=";">
            update s_sys_process_task_config
            <trim prefix="SET" suffixOverrides=",">
                client_id = #{clientId},
                id = #{id},
                process_key = #{processKey},
                task_id = #{taskId},
                task_name = #{taskName},
                properties_id = #{propertiesId},
                remark = #{remark},
                create_date = #{createDate},
                modify_date = #{modifyDate},
                creator_account = #{creatorAccount},
                updater_account = #{updaterAccount},
                create_by_id = #{createById},
                modify_by_id = #{modifyById},
                status = #{status},
            </trim>
            where id = #{id}
        </foreach>
    </update>

</mapper>
